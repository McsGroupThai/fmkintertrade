# คู่มือนำขึ้นเว็บจริง — FMK Intertrade CMS

**อ่านให้จบก่อนเริ่ม แล้วทำตามลำดับ อย่าข้ามขั้น**

สถานะ: ยังไม่เคยขึ้นเว็บจริง · ยังไม่เคยรันบน Apache ของโฮสต์นี้
ชุดไฟล์อยู่ในโฟลเดอร์ `dist/` สร้างใหม่ได้ด้วย `php bin/make-bundle.php`

> **ไม่มีใครรับประกันได้ว่าการขึ้นเว็บจริงจะไม่มีความเสี่ยง**
> สิ่งที่ทำได้คือทำให้ความเสียหายกลับคืนได้ทุกขั้น ซึ่งคือเหตุผลของลำดับด้านล่างนี้

---

## สรุปแนวคิดของลำดับนี้

ทุกขั้นถูกเรียงให้ **เว็บจริงไม่ถูกแตะจนถึงขั้นสุดท้าย**

ไฟล์ `index.html` ซึ่งเป็นหน้าเว็บที่ลูกค้าเห็น **ไม่อยู่ในชุดติดตั้ง** โดยตั้งใจ
ระบบจะเขียนไฟล์นั้นเองตอนคุณกดปุ่ม "เผยแพร่" ในขั้นที่ 9
แปลว่าถ้ามีอะไรผิดพลาดในขั้นที่ 1 ถึง 8 **เว็บที่ลูกค้าเห็นยังเหมือนเดิมทุกประการ**

และเพราะหน้าเว็บสาธารณะเป็นไฟล์ static ไฟล์เดียว ไม่พึ่ง PHP ไม่พึ่งฐานข้อมูล
ต่อให้หลังบ้านพังทั้งระบบทีหลัง เว็บก็ยังเปิดได้ตามปกติ

---

## สิ่งที่ต้องเตรียม

| | |
|---|---|
| เข้าถึงอะไรได้ | DirectAdmin File Manager + phpMyAdmin (ไม่มี SSH ไม่มี FTP) |
| ต้องใช้เวลา | ประมาณ 40–60 นาที ถ้าไม่ติดอะไร |
| ต้องมีที่ว่างในเครื่อง | สำหรับดาวน์โหลดไฟล์สำรอง `public_html` ทั้งโฟลเดอร์ |

**ห้ามส่งรหัสผ่าน DirectAdmin หรือรหัสฐานข้อมูลลงแชต** ไม่ว่ากรณีใด
รหัสเหล่านั้นพิมพ์บนโฮสต์อย่างเดียว

---

## ขั้นที่ 1 — สำรองข้อมูล (ห้ามข้าม)

1. DirectAdmin → **File Manager** → เลือกโฟลเดอร์ `public_html` → **Compress** เป็น zip
2. **ดาวน์โหลด zip นั้นลงเครื่อง** อย่าเก็บไว้แต่บนเซิร์ฟเวอร์ — ถ้าเซิร์ฟเวอร์มีปัญหา ไฟล์สำรองจะหายไปด้วย
3. จดไว้: `index.html` เดิมขนาดกี่ไบต์ แก้ไขล่าสุดวันไหน
4. ถ้ามีฐานข้อมูลอยู่แล้ว → phpMyAdmin → **Export** → เก็บลงเครื่องด้วย
   (ตอนตรวจครั้งล่าสุดโฮสต์นี้ยังไม่มีฐานข้อมูลเลย — Database Count 0/1)

> ในเครื่องพัฒนามีไฟล์ `index.original.html` ซึ่งเป็นสำเนาเว็บเดิมที่ไม่เคยถูกแก้
> ตรวจสอบแล้วว่าตรงกับ commit `71f82c2` ใน repo ต้นฉบับทุกไบต์ ใช้เป็นตัวกู้คืนสำรองได้อีกทาง

---

## ขั้นที่ 2 — ตรวจสภาพโฮสต์ (ก่อนแตะอะไรทั้งสิ้น)

ขั้นนี้ไม่เปลี่ยนอะไรบนเซิร์ฟเวอร์เลย แค่ถามว่าโฮสต์พร้อมไหม

1. อัปโหลด `dist/fmk-check.php` ไปไว้ที่ `public_html/fmk-check.php`
2. เปิดลิงก์ที่อยู่ในไฟล์ `dist/UPLOAD-ME.txt`
   (รูปแบบ `https://fmkintertrade.shop/fmk-check.php?k=<กุญแจ>` — ไม่ใส่กุญแจจะได้ 404)
3. อ่านผล

   - **แถบเขียว** → ไปขั้นต่อไปได้
   - **แถบส้ม** → ไปต่อได้ แต่ต้องอ่านว่าจะเสียความสามารถอะไร
   - **แถบแดง** → **หยุด** แก้ให้หมดก่อน โดยมากแก้ได้ที่ DirectAdmin → *Select PHP Version*

4. **จดค่า "โฟลเดอร์ที่ไฟล์นี้อยู่" กับ "โฟลเดอร์ชั้นเหนือขึ้นไป" ไว้** จะใช้ในขั้นที่ 5

ตอนนี้ยังอย่าเพิ่งลบ `fmk-check.php` — จะใช้อีกครั้งในขั้นที่ 8

---

## ขั้นที่ 3 — สร้างฐานข้อมูล

DirectAdmin → **MySQL Management** → Create new Database

- ชื่อฐานข้อมูลและชื่อผู้ใช้จะถูกเติม prefix ของบัญชีให้อัตโนมัติ (เช่น `zporlszw_fmkcms`)
- **ตั้งรหัสผ่านใหม่ ไม่ซ้ำกับรหัสอื่นใดที่เคยใช้** และห้ามใช้ซ้ำกับเครื่องพัฒนา
- จดชื่อฐานข้อมูล / ชื่อผู้ใช้ / รหัสผ่าน ไว้ในที่ปลอดภัย (ตัวจัดการรหัสผ่าน ไม่ใช่ไฟล์ข้อความ)

---

## ขั้นที่ 4 — วางโค้ดหลังบ้าน ไว้ "นอก" โฟลเดอร์เว็บ

นี่คือขั้นที่สำคัญที่สุดเรื่องความปลอดภัย ถ้าวางผิดที่ คนทั่วไปจะเปิดอ่านไฟล์ที่มีรหัสผ่านได้

โครงที่ต้องได้:

    domains/fmkintertrade.shop/
    ├── app/              <-- โค้ดแกนกลาง
    ├── config/           <-- ไฟล์ตั้งค่า มีรหัสผ่าน
    ├── db/
    ├── bin/
    ├── template.html
    ├── content.json
    └── public_html/      <-- โฟลเดอร์เว็บ อยู่ชั้นในสุด

1. File Manager → เข้าไปที่ `domains/fmkintertrade.shop/`
   (ให้เห็น `public_html` อยู่ในนั้น — ต้อง **ไม่ได้อยู่ข้างใน** `public_html`)
2. อัปโหลด `dist/fmk-app.zip` ลงโฟลเดอร์นี้ → คลิกขวา → **Extract**
3. ลบไฟล์ zip ทิ้ง
4. ตรวจว่าเห็น `app/`, `config/`, `db/`, `bin/`, `template.html`, `content.json` เรียงอยู่ข้าง ๆ `public_html`

> **ถ้าโฮสต์ไม่ยอมให้วางนอก `public_html` จริง ๆ**
> ในแต่ละโฟลเดอร์มี `.htaccess` ที่ปฏิเสธการเข้าถึงติดมาให้แล้ว แต่นั่นเป็นด่านสำรอง ไม่ใช่ด่านหลัก
> และต้องแก้บรรทัด `require_once dirname(__DIR__, 2) . '/app/bootstrap.php';`
> ในไฟล์ `public_html/admin/*.php` ทุกไฟล์ให้ชี้ตำแหน่งจริง

---

## ขั้นที่ 5 — เขียนไฟล์ตั้งค่า

1. File Manager → `config/` → คัดลอก `config.example.php` เป็น `config.local.php`
2. แก้ไขไฟล์ `config.local.php` (Edit ในหน้าเว็บ) ใส่ค่าจริง:

| ช่อง | ใส่อะไร |
|---|---|
| `env` | `'production'` — ปิดการแสดง error ให้ผู้เข้าชมเห็นโดยอัตโนมัติ |
| `db.host` | `'localhost'` |
| `db.port` | `3306` |
| `db.database` / `db.user` / `db.password` | ค่าจากขั้นที่ 3 |
| `public_dir` | ที่อยู่เต็มของ `public_html` — ค่าที่จดไว้จากขั้นที่ 2 |
| `contact_notify_email` | ปล่อยว่างไว้ก่อน เปิดทีหลังเมื่อทดสอบเมลของโฮสต์แล้ว |

**`public_dir` คือช่องที่พลาดแล้วเจ็บที่สุด** ถ้าเว้นว่างไว้ ระบบจะไปหาโฟลเดอร์ชื่อ `public/`
ซึ่งบนโฮสต์ไม่มี การกดเผยแพร่จะเขียนไฟล์ผิดที่หรือฟ้อง error ทั้งที่เนื้อหาถูกต้องแล้ว
(ตั้งค่าเป็นโฟลเดอร์ที่ไม่มีอยู่จริง ระบบจะฟ้องทันที ไม่ปล่อยผ่านเงียบ ๆ)

3. ตรวจว่า `config.local.php` **ไม่ได้อยู่ใน** `public_html`

---

## ขั้นที่ 6 — นำเข้าฐานข้อมูล

1. DirectAdmin → **phpMyAdmin** → เลือกฐานข้อมูลที่สร้างไว้จากเมนูซ้าย
2. แท็บ **Import** → Choose File → เลือก `dist/fmk-install.sql` → **Go**
3. ต้องได้ 10 ตาราง: `users`, `sessions`, `login_attempts`, `audit_log`,
   `content_draft`, `content_versions`, `media`, `contact_messages`, `contact_blocked`, `schema_migrations`
4. คลิกตาราง `content_draft` → ต้องมี 1 แถว คือเนื้อหาเว็บทั้งหมด

ไฟล์นี้รวมทุกอย่างไว้ก้อนเดียว เรียงลำดับถูกต้องแล้ว และรันซ้ำได้โดยไม่ทำข้อมูลเสีย
(ใช้ `CREATE TABLE IF NOT EXISTS` ทั้งหมด)

---

## ขั้นที่ 7 — วางไฟล์เว็บ

1. File Manager → เข้า `public_html`
2. อัปโหลด `dist/fmk-public.zip` → **Extract**
3. ลบไฟล์ zip ทิ้ง

สิ่งที่จะได้เพิ่มเข้ามา: `admin/`, `assets/`, `contact.php`, `.htaccess`,
`robots.txt`, `logo-full.jpeg`, `logo-monogram.png`, `logo-seal.jpeg`

**`index.html` เดิมยังอยู่ครบ ไม่ถูกแตะ — เว็บที่ลูกค้าเห็นยังเหมือนเดิมทุกประการ**

> `.htaccess` เป็นไฟล์เดียวในขั้นนี้ที่ทำให้เว็บล่มได้ (ขึ้น error 500)
> **อัปเสร็จแล้วให้เปิด `https://fmkintertrade.shop/` ทันที**
> ถ้าขึ้น 500 → ลบ `public_html/.htaccess` ทิ้ง เว็บจะกลับมาทันที แล้วแจ้งว่าเจออะไร

---

## ขั้นที่ 8 — ตรวจอีกรอบ

เปิดลิงก์ `fmk-check.php?k=...` เดิมอีกครั้ง คราวนี้ต้องได้เพิ่ม:

- [ ] `.htaccess` ถูกอ่านจริง → **ผ่าน**
- [ ] ต่อฐานข้อมูล → **ผ่าน** และขึ้นว่าตารางครบ
- [ ] PHP เขียนไฟล์ในโฟลเดอร์เว็บได้ → **ผ่าน**
- [ ] เขียนทับ index.html เดิมได้ → **ผ่าน**
- [ ] เปิดผ่าน HTTPS → **ผ่าน**

ถ้าข้อ "เขียนทับ index.html เดิมได้" ไม่ผ่าน **หยุดตรงนี้** ขั้นที่ 9 จะไม่สำเร็จ
ให้แก้สิทธิ์ไฟล์ก่อน (ไฟล์ 644 · โฟลเดอร์ 755 · เจ้าของเป็นผู้ใช้เดียวกับที่ PHP ทำงาน)

---

## ขั้นที่ 9 — สร้างผู้ดูแลคนแรก แล้วลบเครื่องมือทิ้ง

โฮสต์นี้ไม่มี SSH จึงสร้างบัญชีแรกผ่านหน้าเว็บ โดยใช้วิธีพิสูจน์ว่าคุณคือเจ้าของพื้นที่จริง

1. อัปโหลด `dist/recover.php` ไปไว้ที่ `public_html/admin/recover.php`
2. เปิด `https://fmkintertrade.shop/admin/recover.php`
3. หน้าจอจะบอกชื่อไฟล์แบบสุ่ม เช่น `fmk-a1b2c3d4e5f6.txt`
   → กลับไป File Manager สร้างไฟล์เปล่าชื่อนั้นใน `public_html/admin/` → กด **ตรวจอีกครั้ง**
   (คนที่ไม่มีสิทธิ์เข้า File Manager ทำขั้นนี้ไม่ได้ นั่นคือจุดประสงค์)
4. ใส่อีเมลและรหัสผ่านของผู้ดูแลคนแรก — **ต้องเป็นคนละรหัสกับ DirectAdmin**
5. **ลบ `public_html/admin/recover.php` ทิ้งทันที**
   ถ้าลืม หลังบ้านจะขึ้นแถบเตือนสีแดงทุกหน้าจนกว่าจะลบ
6. **ลบ `public_html/fmk-check.php` ทิ้งด้วย**

---

## ขั้นที่ 10 — เผยแพร่ครั้งแรก (นี่คือจุดที่เว็บเปลี่ยนจริง)

1. เข้า `https://fmkintertrade.shop/admin/login.php` ด้วยบัญชีจากขั้นที่ 9
2. ไปที่ **เผยแพร่และประวัติ** → กด **ดูตัวอย่าง** ก่อน
3. ไล่ดูให้ครบทั้งภาษาไทยและอังกฤษ — เนื้อหาต้องเหมือนเว็บปัจจุบันทุกจุด
   ต่างจากเดิมแค่เรื่องเดียวคือ **ฟอร์มติดต่อส่งข้อมูลจริงแล้ว ไม่ใช่ตัวอย่าง**
4. พอใจแล้วกด **เผยแพร่**

ระบบจะเขียน `public_html/index.html` ใหม่ ณ จุดนี้เว็บจริงเปลี่ยนแล้ว

---

## ขั้นที่ 11 — ตรวจหลังเปิดใช้

เนื้อหาและหน้าตา

- [ ] เปิด `https://fmkintertrade.shop/` — เหมือนเดิมทุกจุด ทั้ง EN และ TH
- [ ] เปิดบนมือถือจริง — เมนู การสลับภาษา ปุ่มเปิดฟอร์ม ทำงานปกติ
- [ ] โลโก้ทุกจุดขึ้นครบ ไม่มีรูปแตก

ฟอร์มติดต่อ

- [ ] ส่งข้อความทดสอบหนึ่งครั้ง → ขึ้นข้อความขอบคุณ
- [ ] หลังบ้าน → **กล่องข้อความ** → เห็นข้อความนั้น อ่านครบทุกช่อง
- [ ] ลบข้อความทดสอบทิ้ง

ความปลอดภัย — ทุกข้อต้อง **เข้าไม่ได้**

- [ ] `https://fmkintertrade.shop/app/Auth.php` → 403 หรือ 404 ห้ามเห็นโค้ด
- [ ] `https://fmkintertrade.shop/config/config.local.php` → เข้าไม่ได้
- [ ] `https://fmkintertrade.shop/content.json` → เข้าไม่ได้
- [ ] `https://fmkintertrade.shop/admin/recover.php` → 404 (ลบไปแล้ว)
- [ ] `https://fmkintertrade.shop/fmk-check.php` → 404 (ลบไปแล้ว)

หลังบ้าน

- [ ] ใส่รหัสผิด 6 ครั้ง → ต้องถูกล็อก แล้วรอ 15 นาทีจึงลองใหม่ได้
- [ ] เปิด DevTools → Application → Cookies → คุกกี้ `FMKSESS` ต้องติดธง **Secure** และ **HttpOnly**
- [ ] แก้ข้อความ 1 จุด → ดูตัวอย่าง → เผยแพร่ → หน้าเว็บเปลี่ยนจริง
- [ ] อัปโหลดรูป 1 รูปเข้าคลังรูป → ขึ้นรูปย่อได้
- [ ] กดย้อนกลับเวอร์ชัน → ได้ฉบับร่างเดิมกลับมา
- [ ] สร้างบัญชีที่สองไว้เป็นตัวสำรอง เผื่อบัญชีแรกเข้าไม่ได้

---

## แผนย้อนกลับ

| อาการ | ทำอะไร |
|---|---|
| เว็บขึ้น error 500 หลังอัป `.htaccess` | ลบ `public_html/.htaccess` — กลับทันที |
| หน้าเว็บเพี้ยนหลังกดเผยแพร่ | หลังบ้าน → เผยแพร่และประวัติ → **ย้อนกลับ** ไปเวอร์ชันก่อนหน้า แล้วกดเผยแพร่ |
| เผยแพร่แล้วพังจนเข้าหลังบ้านไม่ได้ | อัป `index.original.html` ทับ `public_html/index.html` — เว็บกลับเป็นแบบเดิมทันที |
| อยากปิดหลังบ้านชั่วคราว | ลบโฟลเดอร์ `public_html/admin/` — เว็บสาธารณะไม่กระทบเลย |
| อยากกลับทั้งหมด | แตก zip จากขั้นที่ 1 ทับกลับไป |
| ฐานข้อมูลมีปัญหา | ลบทิ้งได้ ไม่กระทบหน้าเว็บสาธารณะ เพราะหน้าเว็บเป็นไฟล์ static |

---

## สิ่งที่ยัง **ไม่เคย** ถูกทดสอบ — รู้ไว้ก่อนตัดสินใจ

ชุดทดสอบทั้งหมด (รวม 373 ข้อ) รันบนเซิร์ฟเวอร์ในตัวของ PHP กับ DOM จำลอง
สิ่งต่อไปนี้จึงยังเป็นการคาดการณ์ ไม่ใช่ผลทดสอบ:

| เรื่อง | ทำไมยังไม่รู้ |
|---|---|
| `.htaccess` บน Apache จริง | เซิร์ฟเวอร์ในตัวของ PHP ไม่อ่าน `.htaccess` เลย — `fmk-check.php` ตรวจให้ได้ว่าถูกอ่านไหม แต่ไม่ได้ตรวจทุกบรรทัด |
| หน้าตาบนเบราว์เซอร์จริงและมือถือจริง | ทดสอบด้วย DOM จำลอง ไม่ได้เปิดดูด้วยตา |
| โปรแกรมอ่านหน้าจอ | เขียนตามมาตรฐาน ARIA แต่ยังไม่เคยลองกับ NVDA หรือ VoiceOver |
| `mail()` ของโฮสต์ | จึงตั้งค่าแจ้งเตือนอีเมลเป็นปิดไว้ก่อน |
| ฐานข้อมูลบน MySQL ของโฮสต์ | ทดสอบบน MariaDB 10.11 · `fmk-install.sql` ตรวจแล้วว่ารันผ่านและรันซ้ำได้ แต่ทดสอบบนฐานข้อมูลที่มีตารางอยู่แล้ว ไม่ใช่ฐานข้อมูลเปล่า (ลำดับคำสั่งเป็นลำดับเดียวกับที่สร้างฐานข้อมูลในเครื่องพัฒนาตอนแรก) |
| ปริมาณผู้ใช้จริง | ยังไม่เคยมีคนใช้พร้อมกันหลายคน |

---

## ความสามารถที่ยังไม่มี

| เรื่อง | สถานะ |
|---|---|
| ลืมรหัสแล้วกู้เองผ่านอีเมล | ไม่มีโดยตั้งใจ — ต้องใช้บริการภายนอก ให้ผู้ดูแลอีกคนตั้งรหัสใหม่ให้แทน หรือใช้ `recover.php` |
| 2FA | ยังไม่มี |
| ซ่อนทั้ง section | ซ่อนได้เฉพาะรายการย่อย |
| ตัดรูป / เลือกจุดโฟกัส | ยังไม่มี — อัปแล้วได้ทั้งรูปตามที่ส่งมา |
| `srcset` สำหรับจอเล็ก | ยังไม่มี — มือถือโหลดรูปขนาดเดียวกับเดสก์ท็อป |

---

## หลังจากนี้ ถ้าจะแก้โค้ด

1. แก้ในเครื่องพัฒนา → รันชุดทดสอบ → `node verify.js`
2. `php bin/make-bundle.php` สร้างชุดใหม่
3. อัปทับเฉพาะไฟล์ที่เปลี่ยน — **อย่าอัป `config.local.php` ทับของบนโฮสต์**
4. ถ้ามีไฟล์ `db/005_*.sql` เพิ่มเข้ามา ต้อง import ผ่าน phpMyAdmin ด้วย

---

## อัปเดตรอบที่ 2 — เก็บปุ่มที่กดแล้วไม่ไปไหน + หน้านโยบาย + บทความ (2026-09-17)

รอบนี้ **ไม่มี** การแก้ฐานข้อมูลเชิงโครงสร้าง ไม่มีไฟล์ `db/*.sql` ใหม่
และไม่แตะไฟล์ตั้งค่าหรือบัญชีผู้ใช้ใด ๆ

### สำรองก่อน

1. phpMyAdmin → เลือกฐานข้อมูล → **Export** → เก็บไฟล์ `.sql` ไว้
2. File Manager → ดาวน์โหลด `template.html` ตัวปัจจุบันเก็บไว้ (ใช้ย้อนกลับ)

### ไฟล์ที่ต้องอัปโหลด

| ไฟล์ | วางที่ | ใหม่/ทับ |
|---|---|---|
| `template.html` | รากโฟลเดอร์หลังบ้าน (ที่เดียวกับ `app/`) | อัปทับ |
| `template-privacy.html` | รากโฟลเดอร์หลังบ้าน | ไฟล์ใหม่ |
| `app/Builder.php` | `app/` | อัปทับ |
| `app/Content.php` | `app/` | อัปทับ |
| `app/Sections.php` | `app/` | อัปทับ |
| `bin/link-footer.php` · `bin/add-privacy.php` · `bin/add-article-body.php` · `bin/snapshot.php` | `bin/` | ใหม่ 3 · ทับ 1 |

และในโฟลเดอร์เว็บ (`public_html`) อีก 3 ไฟล์

| ไฟล์ | วางที่ |
|---|---|
| `admin/preview.php` | อัปทับ — เพิ่มปุ่มดูตัวอย่างหน้านโยบาย |
| `admin/publish.php` | อัปทับ — รายงานจำนวนไบต์ของทั้งสองหน้า |
| `admin/content.php` | อัปทับ — สวิตช์เปิดอ่านบทความต้องตั้งพร้อมกันทั้งสองภาษา |

`content.json` **ไม่ต้องอัป** — เนื้อหาบนเว็บจริงอยู่ในฐานข้อมูล ไม่ได้อ่านจากไฟล์นี้
`privacy.html` **ก็ไม่ต้องอัป** — ระบบจะเขียนให้เองตอนกดเผยแพร่ เหมือน `index.html`

### รันสคริปต์อัปเกรดเนื้อหา — สามคำสั่ง เรียงตามลำดับ

DirectAdmin → **Terminal** → เข้าโฟลเดอร์หลังบ้าน แล้ว

```
php bin/link-footer.php
php bin/add-privacy.php
php bin/add-article-body.php
```

**ต้องรันตามลำดับนี้** เพราะคำสั่งที่สองต้องใส่ลิงก์ลงในช่อง "ลิงก์" ของรายการท้ายเว็บ
ซึ่งช่องนั้นเพิ่งถูกสร้างโดยคำสั่งแรก ถ้าสลับลำดับ คำสั่งที่สองจะฟ้องและหยุดเอง

- คำสั่งแรก — แปลงลิงก์ท้ายเว็บเป็น `{ข้อความ, ลิงก์}` และเปลี่ยนข้อความการ์ดโซลูชันเป็น "สอบถามเรื่องนี้"
- คำสั่งที่สอง — ใส่เนื้อหานโยบายความเป็นส่วนตัว 11 หัวข้อทั้งสองภาษา
  ต่อลิงก์ท้ายเว็บไป `privacy.html` และเพิ่มลิงก์ให้กดอ่านนโยบายใต้ช่องยินยอมในฟอร์ม
- คำสั่งที่สาม — เพิ่มช่อง "เนื้อหาเต็ม" และสวิตช์ "เปิดให้กดอ่านเนื้อหาเต็ม" ให้บทความทั้งสามชิ้น
  **ปิดไว้ทั้งหมด** หน้าเว็บจึงยังไม่มีคำว่า "อ่านต่อ" ให้กดเลย

ทั้งสองคำสั่ง **รันซ้ำได้ ไม่พัง** และจะไม่ทับข้อความที่ผู้ดูแลแก้เองไว้แล้ว
แก้แค่ **ฉบับร่าง** เท่านั้น หน้าเว็บจริงยังไม่เปลี่ยนจนกว่าจะกดเผยแพร่

### ตรวจแล้วค่อยเผยแพร่

1. เข้าหลังบ้าน → **ดูตัวอย่าง**
2. เลื่อนไปหัวข้อโซลูชัน → ปุ่มต้องเขียนว่า "สอบถามเรื่องนี้" → กดแล้วฟอร์มต้องเปิดพร้อมเลือกโซลูชันใบนั้นไว้ให้
3. ในฟอร์ม ใต้ช่องยินยอม ต้องมีลิงก์ "อ่านนโยบายความเป็นส่วนตัว"
4. เลื่อนลงท้ายเว็บ → คอลัมน์ "บริษัท" 3 ลิงก์ · แถวล่างมี "นโยบายความเป็นส่วนตัว"
5. กดปุ่ม **ดูหน้านโยบาย** บนแถบตัวอย่าง → ต้องเห็นเอกสาร 11 หัวข้อ สลับ EN/TH ได้
6. เลื่อนไปหัวข้อบทความ → การ์ดทั้งสามใบต้อง **ไม่มี** คำว่า "อ่านต่อ" (เพราะยังปิดอยู่)
7. พอใจแล้วกด **เผยแพร่** — ข้อความยืนยันต้องบอกทั้ง `index.html` และ `privacy.html`
8. เปิด `https://fmkintertrade.shop/privacy.html` ตรง ๆ อีกรอบเพื่อยืนยันว่าไฟล์ถูกเขียนจริง

### ก่อนเผยแพร่ ให้ยืนยันสองเรื่องในเนื้อหานโยบาย

นโยบายเขียนจากสิ่งที่ระบบทำจริง แต่มีสองจุดที่เป็นการตัดสินใจของบริษัท ไม่ใช่ของนักพัฒนา

1. **หัวข้อ 7 ระยะเวลาเก็บข้อมูล** — ตอนนี้เขียนว่า 2 ปี และ 90 วัน แก้ได้ที่
   หลังบ้าน → แก้ไขเนื้อหา → **หน้านโยบาย**
2. **ที่ตั้งเซิร์ฟเวอร์** — ถ้าโฮสต์อยู่นอกประเทศไทย ต้องเพิ่มข้อความแจ้งการส่งข้อมูลข้ามแดน

เอกสารนี้ไม่ใช่ความเห็นทางกฎหมาย ควรให้ผู้มีความรู้ทางกฎหมายอ่านก่อนใช้จริง

### วิธีเปิดบทความให้กดอ่านได้ (ทำเมื่อไรก็ได้ หลังขึ้นเว็บแล้ว)

1. หลังบ้าน → **แก้ไขเนื้อหา** → **รายการบทความ** → เลือกบทความที่ต้องการ
2. ที่ช่อง **ย่อหน้า** กด "+ เพิ่มรายการ" แล้วพิมพ์เนื้อหาทีละย่อหน้า ทั้งภาษาไทยและอังกฤษ
3. ติ๊ก **เปิดให้กดอ่านเนื้อหาเต็ม**
4. กด **บันทึก** → **ดูตัวอย่าง** → **เผยแพร่**

การ์ดใบนั้นจะมีคำว่า "อ่านต่อ" ขึ้นมา กดแล้วเปิดหน้าต่างอ่านบทความในหน้าเดิม

**ถ้าติ๊กเปิดแต่ยังไม่ได้พิมพ์ย่อหน้าเลย การ์ดจะยังไม่มีปุ่มให้กด** — ตั้งใจให้เป็นแบบนั้น
เพราะกดแล้วเจอหน้าต่างเปล่าแย่กว่าไม่มีปุ่ม

เนื้อหาของบทความที่ยังปิดอยู่ **ไม่ถูกส่งไปหน้าเว็บเลย** แม้แต่ในซอร์สโค้ด
เขียนค้างไว้ได้โดยไม่มีใครเห็นและ Google ไม่เก็บไปเข้าดัชนี

### ถ้าผิดพลาด

| อาการ | ทำอย่างไร |
|---|---|
| หน้าเว็บเพี้ยนหลังเผยแพร่ | หลังบ้าน → **ประวัติเวอร์ชัน** → ย้อนกลับเวอร์ชันก่อนหน้า (หน้าเว็บกลับทันที) |
| กดเผยแพร่แล้วขึ้น error | อัป `template.html` ตัวเดิมที่ดาวน์โหลดไว้กลับไป แล้วเผยแพร่ใหม่ |
| เนื้อหาในฉบับร่างเพี้ยน | Import ไฟล์ `.sql` ที่ export ไว้กลับเข้า phpMyAdmin |
| หน้านโยบายขึ้น 404 | แปลว่ายังไม่ได้กดเผยแพร่ หรือ `template-privacy.html` ยังไม่ได้อัป — ตรวจสองอย่างนี้แล้วเผยแพร่ใหม่ |
| กดเผยแพร่แล้วฟ้องว่าอ่าน `template-privacy.html` ไม่ได้ | ไฟล์ยังไม่ได้อัป หรืออัปผิดโฟลเดอร์ — ต้องอยู่ที่เดียวกับ `template.html` · หน้าเว็บเดิมยังไม่ถูกแตะ |
