# FMK Intertrade — Project Guide

เอกสารบริบทกลางของโปรเจกต์ สำหรับให้คน/AI อ่านแล้วทำงานต่อได้
อัปเดตล่าสุด: 2026-09-16 · สถานะ: **Phase 1, 2, 3, 4 และส่วนเผยแพร่ของ 6 เสร็จ · มีลิงก์เข้าหลังบ้าน (ปิดไว้) — เหลือ Phase 5 (ฟอร์มติดต่อ) ก่อนขึ้นเว็บจริง**
ยังไม่แตะเว็บจริงแม้แต่ไบต์เดียว

---

## 1. ขอบเขตและข้อห้าม

**เป้าหมาย:** คงหน้าตาและเนื้อหาเว็บ https://fmkintertrade.shop/ ตามเดิม แล้วเพิ่มระบบหลังบ้าน (CMS) ให้ผู้ดูแลแก้เนื้อหาได้เองโดยไม่ต้องแก้โค้ด

**แยกขาดจากระบบอื่น:** โปรเจกต์นี้ไม่เกี่ยวกับ MCS Commerce OS และ Coffee POS — ห้ามนำโค้ด ฐานข้อมูล บัญชี หรือเอกสารของสองระบบนั้นมาใช้ร่วม

**ข้อห้ามที่ยังบังคับใช้อยู่:**
- ไม่แก้ / อัปโหลดทับ / ลบไฟล์บนเว็บจริง
- ไม่ติดตั้ง WordPress ทับเว็บเดิม (โฮสต์มี WordPress Manager ไม่ได้แปลว่าเว็บเป็น WordPress — เว็บนี้ **ไม่ใช่** WordPress ยืนยันแล้วจากโค้ด)
- ไม่สร้าง/แก้ฐานข้อมูลบนโฮสติ้งโดยไม่ได้รับอนุมัติ
- ไม่เปลี่ยน DNS ไม่ซื้อบริการเพิ่ม
- ไม่ขอรหัสผ่าน DirectAdmin หรือไฟล์ config ที่มี credential ลงแชต
- ไม่รับประกันว่า deployment ไม่มีความเสี่ยง

---

## 2. สิ่งที่ตรวจสอบแล้ว (verified)

### 2.1 ไฟล์ในโฟลเดอร์โปรเจกต์

| ไฟล์ | ขนาด | หมายเหตุ |
|---|---|---|
| index.html | 86,163 bytes · 674 บรรทัด | หน้าเว็บทั้งหมดในไฟล์เดียว |
| logo-monogram.png | 101,704 bytes | ใช้ใน header + footer |
| logo-seal.jpeg | 139,990 bytes | favicon + การ์ด About |
| readme.txt | 7,370 bytes | บรีฟงาน |
| FMK_PROJECT_GUIDE.md | — | เอกสารนี้ |

ไม่มี `AGENTS.md` ในโฟลเดอร์

### 2.2 เว็บจริง (ตรวจแบบอ่านอย่างเดียว ไม่แตะไฟล์ใด ๆ)

- `HTTP/1.1 200 OK` · `Server: Apache/2` · รองรับ HTTP/2 (`Upgrade: h2,h2c`)
- `Content-Length: 86163` = ขนาดเท่ากับ `index.html` ในเครื่องเป๊ะ → **ไฟล์ในเครื่องตรงกับเว็บจริง**
- `Last-Modified: Sun, 26 Jul 2026 15:50:16 GMT`
- `https://fmkintertrade.shop/logo-seal.jpeg` → **200 OK**
- `https://fmkintertrade.shop/logo-full.jpeg` → **404 Not Found** ⚠️

### 2.3 โครงสร้าง index.html

เป็นไฟล์เดียวจบ ไม่มี build step ไม่มี framework ไม่มีการเรียก backend

| ส่วน | บรรทัด | หมายเหตุ |
|---|---|---|
| `<head>` + meta / OG / Twitter / JSON-LD | 1–36 | SEO + structured data ฝังตายตัว |
| `<style>` CSS ทั้งหมด | 37–116 | inline ในไฟล์ |
| `<body>` = `<div id="page">` + `<div id="layer">` ว่างเปล่า | 117–121 | **DOM ทั้งหมดสร้างด้วย JS** |
| `COMPANY` — ที่อยู่ / โทร / อีเมล / โซเชียล | 123–135 | |
| `ICON_PATHS` / `SOCIAL_PATHS` — SVG | 137–166 | |
| `T` — ข้อความสองภาษา en / th | 169–254 | **หัวใจของงาน CMS** |
| `state` | 255–262 | lang, menuOpen, modalOpen, form, errors |
| ฟังก์ชัน `render*()` 13 ตัว | 267–535 | ต่อ string HTML แล้วยัดด้วย innerHTML |
| ฟอร์ม + validate | 536–635 | |
| event listeners + boot | 641–673 | |

**สำคัญ:** หน้าเว็บ render ฝั่ง client 100% — ถ้าปิด JS จะเห็นหน้าว่าง และ crawler บางตัวอาจเก็บเนื้อหาไม่ได้

### 2.4 ยืนยันจากโค้ด

- **ไม่มี** ระบบ Login / Admin / session / cookie ใด ๆ
- **ไม่มี** `fetch()`, `XMLHttpRequest`, `localStorage`, API endpoint
- **ไม่มี** PHP / **ไม่ใช่** WordPress
- ฟอร์มติดต่อเป็น **demo จริง** — บรรทัด 664–665:
  `/* DEMO ONLY — no backend. Replace this timeout with a real fetch() POST. */`
  ตามด้วย `setTimeout(...)` 900 ms แล้วโชว์ข้อความสำเร็จ
  → ข้อมูลลูกค้าที่กรอก **หายไปทั้งหมด** ไม่ถูกส่งไปไหน
  → UI มีป้าย `demoBadge: 'Demo form — backend integration required'` แสดงอยู่บนเว็บจริง
- `logo-full.jpeg` ถูกอ้างใน `og:image`, `twitter:image` และ `schema.org` `image` (รวม 3 จุด) แต่ไฟล์ **ไม่มีอยู่จริง (404)** → แชร์ลิงก์บน Facebook / LINE / X จะไม่ขึ้นรูป
- ไฟล์รูปที่มีจริงและใช้จริงในหน้า: `logo-monogram.png` (header/footer), `logo-seal.jpeg` (favicon + About)
- โหลดฟอนต์จาก Google Fonts (external dependency: Manrope, Noto Sans Thai, IBM Plex Mono)

### 2.5 โฮสติ้ง (จากภาพหน้าจอ DirectAdmin 2026-09-16)

| หัวข้อ | ค่าที่ยืนยัน | ผลต่อการออกแบบ |
|---|---|---|
| ฐานข้อมูล | **MariaDB 10.11.18-MariaDB-cll-lve** | ✅ ทันสมัย รองรับ JSON column, CTE, window function ครบ |
| Hostname | `localhost` ผ่าน UNIX socket `/var/lib/mysql/mysql.sock` | ✅ ต่อจากในเครื่องเซิร์ฟเวอร์เท่านั้น ปลอดภัยกว่า TCP |
| SQL Mode | `STRICT_TRANS_TABLES`, `ERROR_FOR_DIVISION_BY_ZERO`, `NO_AUTO_CREATE_USER`, `NO_ENGINE_SUBSTITUTION` | ⚠️ strict mode — ต้องกำหนด default ให้ทุกคอลัมน์ ห้ามพึ่ง implicit default |
| **จำนวนฐานข้อมูล** | **Database Count 0/1** — ยังไม่มีสักตัว และสร้างได้ **แค่ 1 ตัว** | ⚠️ **ข้อจำกัดสำคัญ** ดูข้อ 5.1 |
| Prefix ชื่อ DB/user | `zporlszw_` (บังคับตาม DirectAdmin) | ชื่อจริงจะเป็น `zporlszw_fmkcms` เป็นต้น |
| phpMyAdmin | มี | ใช้ import schema และ backup ได้ |
| แพลตฟอร์ม | CloudLinux (`cll-lve`) | shared hosting มีลิมิต CPU/RAM/process ต่อบัญชี |
| **PHP** | **8.3 (native / current)** ผ่าน CloudLinux PHP Selector | ✅ ทันสมัย รองรับ typed properties, enum, readonly, `match`, fibers ครบ |
| Site Isolation | **Denied by server administrator** — ปรับไม่ได้ | ทุกโดเมนในบัญชีรันด้วย user เดียวกัน · ไม่กระทบแผน |
| โดเมนในบัญชี | 2 โดเมน: `fmkintertrade.shop` (เว็บจริง) และ `z9710-po220r.ls04.zwhhosting.com` (subdomain แถมของโฮสต์) | 💡 **โดเมนที่สองใช้เป็นเว็บทดสอบได้** ดูข้อ 5.1 |

**⚠️ ข้อจำกัดของ PHP "native":**
CloudLinux แจ้งว่า *"Changing PHP modules and options is impossible for the native version"* — แปลว่าตอนนี้:
- **ดูรายการ extensions ที่เปิดอยู่ไม่ได้** และ **เปิด/ปิด extension เพิ่มไม่ได้**
- ถ้าจำเป็นต้องจัดการ extension ต้องสลับจาก native ไปเป็น alt-PHP รุ่นระบุเวอร์ชัน (เช่น 8.3 แบบไม่ใช่ native) ซึ่ง **เป็นการเปลี่ยนค่าบนโฮสต์จริง ต้องขออนุมัติก่อน**

extension ที่โปรเจกต์ต้องใช้ — `pdo_mysql`, `mbstring`, `json`, `fileinfo`, และ `gd` หรือ `imagick` สำหรับย่อรูป
ปกติ native PHP ของ shared hosting มีครบอยู่แล้ว แต่ **ยังไม่ได้ยืนยันด้วยตา** → ดูวิธีตรวจแบบปลอดภัยในข้อ 4

---

## 3. คลังเนื้อหาที่ต้องย้ายมาแก้ผ่านหลังบ้าน

ทุกรายการมี **2 ภาษา (en/th)** ต้องแก้ได้ทั้งคู่

| กลุ่ม | รายการ | ประเภทการจัดการ |
|---|---|---|
| ข้อมูลบริษัท | ที่อยู่, โทร 2 เบอร์, อีเมล, เวลาทำการ | แก้ฟิลด์ |
| ช่องทางติดต่อ | Facebook, YouTube, LINE, WhatsApp | แก้ URL / เปิด-ปิด |
| Top bar / Header | locations, tagline, โลโก้ | แก้ฟิลด์ + อัปโหลดรูป |
| เมนูนำทาง | 8 รายการ | เพิ่ม / แก้ / ลบ / เรียง |
| Hero | eyebrow, headline, sub, ปุ่ม 2 ปุ่ม + การ์ดคุณค่า 4 ใบ | แก้ฟิลด์ + จัดการรายการ |
| Trust bar | 4 รายการ | เพิ่ม / แก้ / ซ่อน / เรียง |
| About | eyebrow, title, lead, body, badge + หลักการ 4 ข้อ | แก้ฟิลด์ + จัดการรายการ |
| **Solutions** | หัวเรื่อง + **6 บริการ** | เพิ่ม / แก้ / ซ่อน / เรียง ⭐ |
| **Projects** | หัวเรื่อง + **3 โครงการ** | เพิ่ม / แก้ / ซ่อน / เรียง ⭐ |
| Why FMK | หัวเรื่อง + 4 เหตุผล | เพิ่ม / แก้ / ซ่อน / เรียง |
| Network | หัวเรื่อง + **ไทย 4 สาขา** + **ต่างประเทศ 2 แห่ง** + ตัวเลขสถิติ | เพิ่ม / แก้ / ซ่อน / เรียง |
| **Knowledge** | หัวเรื่อง + **3 บทความ** | เพิ่ม / แก้ / ซ่อน / เรียง ⭐ |
| Final CTA | หัวเรื่อง + ปุ่ม | แก้ฟิลด์ |
| Footer | tagline, 3 คอลัมน์ลิงก์, legal 4 ลิงก์, copyright | แก้ฟิลด์ + จัดการลิงก์ |
| ฟอร์ม | label ทุกช่อง, ข้อความ error, ตัวเลือก dropdown 4 ชุด | แก้ฟิลด์ + จัดการตัวเลือก |
| SEO / Meta | title, description, OG, Twitter, JSON-LD | แก้ฟิลด์ |
| คลังรูป | โลโก้, แบนเนอร์, รูปโครงการ / บทความ | อัปโหลด / ลบ |

**ช่องว่างที่เห็นชัด:** ตอนนี้การ์ด Solutions / Projects / Knowledge ใช้ไอคอน SVG ล้วน ยังไม่มีรูปถ่ายจริงเลยสักใบ — ถ้าจะให้หลังบ้านใส่รูปได้ ต้องออกแบบ template การ์ดเพิ่ม (เป็นการเปลี่ยนหน้าตา ต้องขออนุมัติ)

---

## 4. สิ่งที่ยังไม่ยืนยัน — ต้องขอข้อมูลจากผู้ใช้

| หัวข้อ | ทำไมต้องรู้ | ขออะไร | สถานะ |
|---|---|---|---|
| PHP version | เลือก syntax ให้ถูก | ภาพหน้า **PHP Selector** | ✅ ได้แล้ว — **8.3 native** |
| ฐานข้อมูล | เวอร์ชัน + จำนวนที่สร้างได้ | ภาพหน้า **Databases** | ✅ ได้แล้ว (ข้อ 2.5) |
| **PHP extensions** | ต้องมี `pdo_mysql`, `mbstring`, `fileinfo`, `gd`/`imagick` | native version ดูจาก DirectAdmin ไม่ได้ → ดูวิธีตรวจด้านล่าง | ⏳ **ยังขาด** |
| โควตาพื้นที่ / แบนด์วิดท์ | วางแผนคลังรูป | ภาพหน้าสรุปบัญชี (Dashboard) | ⬜ อยากได้ |
| SSL | ต้องมี HTTPS ก่อนทำ Login | ภาพหน้า **SSL Certificates** | ⬜ อยากได้ |
| cgi-bin | มีไฟล์อะไรอยู่ข้างใน | รายการไฟล์ในโฟลเดอร์ | ⬜ อยากได้ |
| Cron jobs | ใช้ทำ backup อัตโนมัติ | มี / ไม่มีเมนู Cron | ⬜ อยากได้ |
| logo-full.jpeg | จะฟื้นไฟล์ หรือแก้ meta ให้ชี้ไฟล์ที่มีอยู่ | ไฟล์ต้นฉบับ ถ้ามี | ⬜ อยากได้ |

> ⚠️ **ห้ามส่งรหัสผ่าน** — ต้องการแค่ชื่อเวอร์ชันและรายการความสามารถ ให้เบลอหรือครอบตัดส่วนที่เป็น username, password และ path เต็ม ออกก่อนส่ง
> (หมายเหตุ: ภาพชุดแรกติด DirectAdmin username มาด้วย ไม่ใช่รหัสผ่านจึงไม่ถือว่ารั่ว แต่ครั้งต่อไปครอบออกได้จะดีกว่า)

### 4.1 วิธีตรวจ PHP extensions แบบปลอดภัย

`phpinfo()` เปิดโล่งบนเว็บจริงเป็นความเสี่ยงด้านความปลอดภัย (เปิดเผย path, เวอร์ชัน, ค่าคอนฟิกให้คนนอกเห็น) จึง **ไม่ควรทำแบบนั้น** ทางเลือกเรียงตามความปลอดภัย:

1. **ถามฝ่ายซัพพอร์ตโฮสต์** — "PHP 8.3 native เปิด extension อะไรบ้าง" ปลอดภัยที่สุด ไม่ต้องแตะเซิร์ฟเวอร์เลย
2. **วางไฟล์ตรวจชั่วคราวบนโดเมนทดสอบ** `z9710-po220r.ls04.zwhhosting.com` (ไม่ใช่เว็บจริง) ที่พิมพ์แค่ผลของ `extension_loaded()` เป็น yes/no ไม่กี่บรรทัด แล้ว **ลบทิ้งทันที** ที่ดูเสร็จ
3. **สลับไป alt-PHP 8.3** ใน PHP Selector เพื่อให้จัดการ extension ได้ — เปลี่ยนค่าบนโฮสต์จริง **ต้องขออนุมัติก่อน** และอาจกระทบเว็บที่รันอยู่

> **ข้อเสนอ: ทางที่ 1** ระหว่างนี้พัฒนาต่อได้เลย เพราะ extension เหล่านี้เป็นของมาตรฐานที่ shared hosting เกือบทุกเจ้าเปิดไว้อยู่แล้ว — ถือเป็น **สมมติฐาน** ที่ยังไม่ยืนยัน

---

## 5. สถาปัตยกรรม

### 5.1 ⚠️ ข้อจำกัด: สร้างฐานข้อมูลได้แค่ 1 ตัว (0/1)

แพ็กเกจโฮสติ้งปัจจุบันให้ฐานข้อมูล **ตัวเดียว** — กระทบ 2 เรื่อง:

**เรื่องที่ 1 — เว็บทดสอบออนไลน์แยกฐานข้อมูลไม่ได้**
readme กำหนดว่า "หากทำเว็บทดสอบออนไลน์ ต้องแยกไฟล์และฐานข้อมูลจากเว็บจริง" แต่มี DB แค่ตัวเดียวจึงทำไม่ได้ตรง ๆ ทางออก:

| ทาง | วิธี | ข้อดี / ข้อเสีย |
|---|---|---|
| **A1** ⭐ | ทดสอบบนเครื่องให้จบ (XAMPP/Laragon ตั้งเป็น PHP 8.3 + MariaDB ให้ตรงโฮสต์) แล้วค่อยขึ้นเว็บจริงทีเดียว | แยกขาด 100% · ไม่มีค่าใช้จ่าย · สภาพแวดล้อมจำลองได้ใกล้เคียงมากเพราะรู้เวอร์ชันแน่ชัดแล้ว |
| **A2** ⭐ | ขึ้นทดสอบบนโดเมนแถม `z9710-po220r.ls04.zwhhosting.com` **ไฟล์แยกคนละ document root** ล็อกด้วย `.htaccess` basic auth · ใช้ DB ตัวเดียวกันแต่แยก **prefix ตาราง** (`fmk_` / `stg_`) | ได้ทดสอบบน PHP/MariaDB ตัวจริง · ไฟล์แยกจริง · แต่ DB **ไม่ได้แยกจริง** ถ้าโค้ดพลาดอาจเขียนข้ามกัน |
| **A3** | ขออัปเกรดแพ็กเกจให้ได้ DB เพิ่ม | แยกจริงทั้งไฟล์และ DB · แต่มีค่าใช้จ่าย ต้องผู้ใช้อนุมัติ (readme ห้ามซื้อเพิ่มเอง) |

> **ข้อเสนอ: A1 เป็นหลัก แล้วใช้ A2 เป็นด่านสุดท้ายก่อน deploy**
> พัฒนาและทดสอบบนเครื่องจนครบ → ขึ้น staging บนโดเมนแถมเพื่อตรวจว่าเข้ากับ PHP/MariaDB ของจริง → ค่อยขึ้นเว็บจริง
> ถ้าเลือก A2 ต้องบังคับ prefix ผ่านค่าคอนฟิกจุดเดียว และ **ห้าม hardcode ชื่อตารางในโค้ด** เพื่อไม่ให้เขียนข้าม environment

**เรื่องที่ 2 — ฐานข้อมูลตัวนี้เป็นของ FMK เท่านั้น**
DB ที่จะสร้าง (`zporlszw_*`) ห้ามใช้ร่วมกับ MCS Commerce OS หรือ Coffee POS เด็ดขาด ตามข้อ 1

### 5.2 ตัวเลือก A — PHP + MariaDB บนโฮสติ้งเดิม ⭐ แนะนำ

โครงไฟล์บนโฮสต์ (เมื่อถึงเวลา deploy เท่านั้น):

    public_html/
    ├── index.html          ← หน้าเว็บที่ generate ออกมาแล้ว (static, เร็ว, SEO ดี)
    ├── assets/             ← รูปที่อัปโหลดผ่านหลังบ้าน
    ├── api/
    │   └── contact.php     ← รับฟอร์มติดต่อจริง
    └── admin/              ← หลังบ้าน (ป้องกันด้วย session + .htaccess)
        ├── login.php
        └── index.php

- แอดมินแก้เนื้อหา → เก็บลง MySQL → กด **Publish** → ระบบ generate `index.html` ใหม่ทับ
- ข้อดี: ใช้โฮสติ้งเดิมได้เลย · หน้าเว็บสาธารณะยังเป็น static ไฟล์เดียวเหมือนเดิม ไม่กระทบความเร็ว/SEO · แอดมินล่มก็ไม่กระทบหน้าบ้าน · backup ง่าย
- ข้อเสีย: ต้องเขียน generator เอง

### 5.3 ตัวเลือก B — PHP + ไฟล์ JSON (ไม่ใช้ฐานข้อมูล)

- เก็บเนื้อหาเป็น `content.json` แทน MySQL
- ข้อดี: ไม่ต้องขออนุมัติสร้างฐานข้อมูล · backup = คัดลอกไฟล์ · ย้ายโฮสต์ง่ายที่สุด
- ข้อเสีย: ประวัติการแก้ไขและการแก้พร้อมกันหลายคน ทำได้จำกัดกว่า
- **เหมาะถ้า:** ไม่ได้รับอนุมัติให้สร้างฐานข้อมูล หรืออยากเก็บ DB slot เดียวที่มีไว้ใช้อย่างอื่น

### 5.4 ตัวเลือก C — Headless CMS ภายนอก (Strapi / Directus / Sanity)

- ข้อเสีย: ค่าใช้จ่ายเพิ่ม · ต้องมีเซิร์ฟเวอร์ใหม่ · ขัดข้อกำหนด "ใช้โฮสติ้งเดิมถ้าเหมาะสม"
- **ไม่แนะนำ** สำหรับงานนี้

### 5.5 สรุป — ตัดสินแล้ว

**ใช้ตัวเลือก A: PHP 8.3 + MariaDB 10.11 บนโฮสติ้งเดิม** ✅

ความสามารถของโฮสต์ยืนยันครบแล้ว: PHP 8.3 (ใหม่พอสำหรับทุกอย่างที่ต้องใช้) · MariaDB 10.11 · DB slot ว่าง 1 ตัวพอดี · phpMyAdmin สำหรับ import/backup · มีโดเมนแถมไว้ทำ staging
โค้ดจะออกแบบให้ storage layer สลับไป **B** (ไฟล์ JSON) ได้ โดยไม่ต้องรื้อส่วนอื่น เผื่อกรณีไม่ได้รับอนุมัติให้สร้าง DB

---

## 6. โครงข้อมูล (ร่าง สำหรับตัวเลือก A)

| ตาราง | ฟิลด์หลัก |
|---|---|
| `users` | id, email, password_hash, role(admin\|editor), status, failed_attempts, locked_until, last_login_at |
| `sessions` | id, user_id, token_hash, ip, user_agent, expires_at |
| `settings` | key, value_en, value_th — ข้อมูลบริษัท, SEO, ช่องทางติดต่อ |
| `sections` | key, title_en, title_th, sub_en, sub_th, visible, sort_order |
| `content_items` | id, section_key, sort_order, visible, icon_key, image_id, title_en, title_th, desc_en, desc_th, meta_json |
| `media` | id, filename, mime, size, width, height, alt_en, alt_th, uploaded_by, created_at |
| `enquiries` | id, full_name, company, position, country, email, phone, solution, project_type, message, contact_method, consent, ip, user_agent, status(new\|read\|replied\|spam), created_at |
| `audit_log` | id, user_id, action, entity, entity_id, diff_json, ip, created_at |
| `publishes` | id, user_id, snapshot_json, published_at — ใช้ย้อนกลับเวอร์ชัน |

---

## 7. แผนดำเนินงาน

| เฟส | งาน | สถานะ |
|---|---|---|
| **0** | สำรวจไฟล์, วิเคราะห์ index.html, ทำเอกสารนี้ | ✅ เสร็จ |
| **0.5** | รวบรวมข้อมูลโฮสติ้ง (ข้อ 4) + เลือกสถาปัตยกรรม | ✅ เสร็จ — เลือก A |
| **1** | แยกเนื้อหาออกจาก `index.html` → `content.json` + `template.html` + `build.js` + `verify.js` | ✅ เสร็จ (ดูข้อ 11) |
| **2** | ระบบ Login ฝั่งเซิร์ฟเวอร์ + session + CSRF + rate limit + สิทธิ์ Admin/Editor | ✅ เสร็จ (ดูข้อ 13) |
| **3** | หน้าจัดการเนื้อหา: ข้อความ 2 ภาษา, บริการ, โครงการ, บทความ (เพิ่ม/แก้/ซ่อน/เรียง) | ✅ เสร็จ (ดูข้อ 14) |
| **4** | คลังรูป + อัปโหลดโลโก้/แบนเนอร์ (ตรวจชนิด + ขนาด + เนื้อหาไฟล์) | ✅ เสร็จ (ดูข้อ 15) |
| **5** | ฟอร์มติดต่อของจริง: บันทึกลงฐานข้อมูล + ส่งอีเมลแจ้งเตือน + กัน spam | ⬜ |
| **6** | Preview ก่อนเผยแพร่ + ปุ่ม Publish + ประวัติการแก้ไข + ย้อนกลับเวอร์ชัน | ✅ เสร็จ (ดูข้อ 14) |
| **7** | ทดสอบบนเครื่อง / บน subdomain ทดสอบที่จำกัดการเข้าถึง | 🔧 สภาพแวดล้อมพร้อมแล้ว (ข้อ 11) |
| **8** | สำรองไฟล์ + DB → แผน rollback → **ขออนุมัติผู้ใช้** → deploy | 📋 เขียนขั้นตอนไว้แล้วใน DEPLOY.md · **ยังไม่ได้ทำ** |

**งานซ่อมแยกต่างหาก (ทำได้เร็ว):** แก้ `logo-full.jpeg` 404 — สร้างไฟล์ขึ้นใหม่ หรือชี้ meta ไปที่ `logo-seal.jpeg`

---

## 8. เช็กลิสต์ความปลอดภัย

- [ ] ตรวจสิทธิ์ทุกครั้งฝั่งเซิร์ฟเวอร์ — ไม่ใช่แค่ซ่อนปุ่มใน UI
- [ ] รหัสผ่าน hash ด้วย `password_hash()` (bcrypt/argon2) — ไม่เก็บ plaintext ไม่ใช้ md5/sha1
- [ ] Session cookie: `HttpOnly` + `Secure` + `SameSite=Strict` + หมุน session id หลัง login
- [ ] CSRF token ทุกฟอร์มที่เปลี่ยนข้อมูล
- [ ] จำกัดการลองรหัสผ่าน (ล็อกชั่วคราวหลังพลาด N ครั้ง) + บันทึก log ความพยายาม
- [ ] อัปโหลดไฟล์: whitelist นามสกุล, ตรวจ MIME จริงด้วย `finfo`, จำกัดขนาด, เปลี่ยนชื่อไฟล์แบบสุ่ม, เก็บนอก webroot หรือปิดการรัน script ด้วย `.htaccess`
- [ ] SQL ใช้ prepared statement ทั้งหมด
- [ ] escape output ทุกจุดที่แสดงข้อมูลผู้ใช้ (กัน XSS)
- [ ] บังคับ HTTPS ทั้ง `/admin`
- [ ] ไม่มี secret ใน frontend, Git, log หรือเอกสารนี้
- [ ] **บัญชีหลังบ้านแยกจากบัญชี DirectAdmin โดยสิ้นเชิง**
- [ ] ฟอร์มติดต่อสาธารณะ: rate limit + honeypot + จำกัดความยาว input

---

## 9. บันทึกการตัดสินใจ

| วันที่ | เรื่อง | ผล |
|---|---|---|
| 2026-09-16 | เว็บเป็น WordPress หรือไม่ | **ไม่ใช่** — HTML/CSS/JS ล้วน ยืนยันจากโค้ด |
| 2026-09-16 | ไฟล์ในเครื่องตรงกับเว็บจริงไหม | **ตรง** — ขนาดไฟล์เท่ากันเป๊ะ 86,163 bytes |
| 2026-09-16 | `logo-full.jpeg` มีจริงไหม | **ไม่มี** — 404 บนเว็บจริง ต้องซ่อม |
| 2026-09-16 | ฟอร์มติดต่อส่งข้อมูลจริงไหม | **ไม่ส่ง** — เป็น setTimeout demo ข้อมูลลูกค้าหายทั้งหมด |
| 2026-09-16 | ฐานข้อมูลใช้ได้ไหม | **ได้** — MariaDB 10.11.18 แต่สร้างได้ **แค่ 1 ตัว** (0/1) |
| 2026-09-16 | PHP เวอร์ชันอะไร | **8.3 native** — ใหม่พอสำหรับทุกอย่างที่ต้องใช้ |
| 2026-09-16 | จัดการ PHP extensions ได้ไหม | **ไม่ได้** ขณะใช้ native version · ต้องสลับไป alt-PHP ซึ่งต้องขออนุมัติ |
| 2026-09-16 | เว็บทดสอบออนไลน์แยก DB ได้ไหม | **แยกไฟล์ได้** (มีโดเมนแถม) แต่ **แยก DB ไม่ได้** → ใช้ prefix ตารางแทน |
| 2026-09-16 | เลือกสถาปัตยกรรม | **ตัดสินแล้ว: A** — PHP 8.3 + MariaDB บนโฮสต์เดิม |
| 2026-09-16 | Phase 1 ยืนยันผลยังไง | เขียน `verify.js` ตรวจ 3 ชั้น (code / data / render) — ผ่านทั้งหมด |

---

## 10. งานถัดไปทันที

1. ⬜ **ตัดสินใจเรื่องป้าย "Demo form" และฟอร์มติดต่อ** ก่อนขึ้นเว็บจริง (ดู DEPLOY.md ข้อ 7)
2. ⬜ ถามซัพพอร์ตโฮสต์เรื่อง PHP extensions ตามข้อ 4.1 (ไม่บล็อกงาน ทำคู่ขนานได้)
3. ⬜ ตัดสินใจเรื่องงานซ่อม 2 ข้อ: `logo-full.jpeg` 404 และป้าย "Demo form" ที่ลูกค้าเห็นอยู่บนเว็บจริง

---

## 11. สภาพแวดล้อมพัฒนาในเครื่อง (ติดตั้งแล้ว 2026-09-16)

### 11.1 ทำไมไม่ใช้ XAMPP ที่มีอยู่

เครื่องนี้มี `C:\xampp` อยู่แล้ว แต่ **ไม่ใช้** ด้วย 2 เหตุผล:

1. **เวอร์ชันไม่ตรงโฮสต์** — XAMPP เป็น PHP 8.2.12 + MariaDB 10.4.32 ส่วนโฮสต์จริงเป็น PHP 8.3 + MariaDB 10.11.18 (ห่างกันหลาย major ในฝั่ง DB)
2. **ชนกับข้อกำหนดแยกระบบ** — `C:\xampp\htdocs` มี `gnc-erp` และ `mcs-data-web` อยู่ ซึ่งเป็นของ MCS ที่ readme สั่งให้แยกขาดจาก FMK

จึงติดตั้งสแตกใหม่แยกต่างหากที่ `C:\fmk-stack` **ไม่แตะ XAMPP เลย**

### 11.2 สิ่งที่ติดตั้ง

| ส่วน | เวอร์ชัน | ตรงกับโฮสต์ |
|---|---|---|
| PHP | **8.3.33** NTS x64 | ✅ โฮสต์ 8.3 |
| MariaDB | **10.11.18** | ✅ ตรงเป๊ะ |
| ที่ตั้ง | `C:\fmk-stack` | — |
| ขนาด | ~360 MB | — |

- ดาวน์โหลดจากต้นทางทางการ (windows.php.net, archive.mariadb.org) และ **ตรวจ SHA-256 ตรงกับที่ผู้ผลิตประกาศทั้งสองไฟล์**
- ติดตั้งแบบ portable **ไม่ต้องสิทธิ์ Administrator** ไม่ลง Windows service ไม่แก้ PATH ลบทิ้งได้ด้วยการลบโฟลเดอร์
- MariaDB ใช้ **พอร์ต 3307** เพื่อไม่ชนกับ XAMPP ที่ใช้ 3306

### 11.3 ค่าที่ตั้งให้ตรงกับโฮสต์

`sql_mode` ตั้งตรงกับที่เห็นใน DirectAdmin เป๊ะ:

    STRICT_TRANS_TABLES, ERROR_FOR_DIVISION_BY_ZERO, NO_AUTO_CREATE_USER, NO_ENGINE_SUBSTITUTION

จงใจตั้งแบบนี้เพื่อให้ **เจอปัญหาตั้งแต่ตอนพัฒนา** แทนที่จะไปโผล่ตอนขึ้นเว็บจริง
character set เป็น `utf8mb4` / `utf8mb4_unicode_ci` ทั้งเซิร์ฟเวอร์และฐานข้อมูล

**PHP extensions ที่เปิดและยืนยันว่าโหลดได้:**
`pdo_mysql` · `mysqli` · `mbstring` · `fileinfo` · `gd` · `openssl` · `curl` · `exif` · `intl` · `zip` · `json` · `session` · `opcache`
(ชุดนี้คือชุดเดียวกับที่ต้องยืนยันกับโฮสต์ตามข้อ 4.1)

### 11.4 ฐานข้อมูลและบัญชี

| รายการ | ค่า |
|---|---|
| ฐานข้อมูล | `fmk_cms` |
| ผู้ใช้แอป | `fmk_app` (มีสิทธิ์เฉพาะ `fmk_cms.*`) |
| host / port | `127.0.0.1` : `3307` |
| รหัสผ่าน | สุ่มจาก RNG ของระบบ เก็บที่ **`C:\fmk-stack\credentials.txt`** |

> 🔐 ไฟล์รหัสผ่านอยู่ **นอกโฟลเดอร์โปรเจกต์โดยตั้งใจ** จะได้ไม่หลุดขึ้น Git หรือถูกอัปโหลดขึ้นเว็บจริงพร้อมโค้ด
> รหัสชุดนี้ใช้กับเครื่องพัฒนาเท่านั้น **ห้ามใช้ซ้ำบนโฮสติ้ง**

### 11.5 คำสั่งใช้งาน

| คำสั่ง | ทำอะไร |
|---|---|
| `C:\fmk-stack\start-db.cmd` | เปิดฐานข้อมูล (ข้ามให้ถ้าเปิดอยู่แล้ว) |
| `C:\fmk-stack\stop-db.cmd` | ปิดฐานข้อมูล (ถามรหัส root) |
| `C:\fmk-stack\db.cmd` | เปิด client ต่อเข้า `fmk_cms` |
| `C:\fmk-stack\serve.cmd` | เปิดเว็บที่ http://localhost:8080 ชี้มาที่โฟลเดอร์โปรเจกต์ |
| `C:\fmk-stack\php.cmd` | เรียก PHP 8.3 ของสแตกนี้ |

### 11.6 ผลทดสอบ

| ทดสอบ | ผล |
|---|---|
| SHA-256 ของไฟล์ที่ดาวน์โหลด ตรงกับต้นทางทางการ | ✅ ทั้ง 2 ไฟล์ |
| PHP 8.3.33 เริ่มทำงานไม่มี startup error | ✅ |
| extensions ที่ต้องใช้โหลดครบ | ✅ 13/13 |
| MariaDB 10.11.18 รับการเชื่อมต่อ | ✅ |
| `sql_mode` ตรงกับโฮสต์ | ✅ |
| PHP → pdo_mysql → MariaDB (prepared statement) | ✅ |
| เก็บ/อ่านข้อความไทยกลับมาตรงเป๊ะ (utf8mb4) | ✅ |
| strict mode ปฏิเสธข้อมูลยาวเกินจริง ไม่ตัดเงียบ | ✅ |
| เสิร์ฟ `index.html` + โลโก้ ผ่าน HTTP | ✅ HTTP 200 |
| XAMPP และโปรเจกต์ MCS ไม่ถูกแตะ | ✅ |

---

## 12. วิธีทำงานกับไฟล์ในโปรเจกต์ (หลัง Phase 1)

### 12.1 ไฟล์และหน้าที่

| ไฟล์ | หน้าที่ | แก้ด้วยมือได้ไหม |
|---|---|---|
| **`content.json`** | **เนื้อหาทั้งหมดของเว็บ** (บริษัท + ข้อความ en/th) — ต่อไปหลังบ้านจะแก้ไฟล์นี้ | ✅ แก้ที่นี่ |
| `template.html` | โครงหน้าเว็บ + CSS + โค้ด render — ไม่มีเนื้อหาอยู่เลย | ✅ แก้เมื่อเปลี่ยนดีไซน์/โครงสร้าง |
| `build.js` | รวม template + content → `index.html` พร้อม validate เนื้อหา | ✅ |
| `verify.js` | ตรวจว่า `index.html` ที่ build ให้ผลเหมือนต้นฉบับ | ✅ |
| **`index.html`** | **ไฟล์ที่ generate ออกมา — ห้ามแก้ด้วยมือ** แก้แล้วจะถูกทับตอน build | ❌ |
| `index.original.html` | ต้นฉบับก่อนแตะอะไร เก็บไว้เทียบและเป็นตัวย้อนกลับ | ❌ อย่าแก้ |

### 12.2 ขั้นตอนแก้เนื้อหา (ตอนนี้ยังทำด้วยมือ)

    1. แก้ content.json
    2. node build.js      -> สร้าง index.html ใหม่ (ถ้าเนื้อหาผิดรูปจะหยุดพร้อมบอกว่าผิดตรงไหน)
    3. node verify.js     -> ตรวจว่าไม่มีอะไรพัง
    4. เปิด index.html ในเบราว์เซอร์เพื่อดูด้วยตา

### 12.3 verify.js ตรวจอะไรบ้าง

| ชั้น | ตรวจ | ผลล่าสุด |
|---|---|---|
| 1. CODE | โค้ดส่วนที่ไม่ใช่เนื้อหา byte-identical กับต้นฉบับ | ✅ |
| 2. DATA | ค่า `COMPANY` และ `T` ที่โค้ดมองเห็น ตรงกับต้นฉบับ (17 คีย์/ภาษา) | ✅ |
| 3. RENDER | HTML ที่ render จริง 4 กรณี (en/th × menu/modal) ตรงกันทุกตัวอักษร (~66,000 ตัวอักษร/กรณี) | ✅ |
| 4. CONTENT | นับข้อความที่สกัดได้ 546 ชิ้น (ไทย 223) กันเนื้อหาหล่น | ✅ |

นอกจากนี้ `build.js` มี validate ที่ทดสอบด้านลบแล้ว 5/5 — จับได้ทั้งคีย์ภาษาขาด, อีเมลว่าง, `social` ผิดชนิด และ `i18n.th` หาย

### 12.4 Phase 6 จะต่อยังไง

PHP บนโฮสต์จะทำหน้าที่แทน `build.js` — อ่าน `content.json` (หรือ MariaDB) แล้ว generate `index.html` ตอนกดปุ่ม Publish
**ตรรกะการ inject ต้องตรงกับ `build.js` เป๊ะ** รวมถึงการ escape `<` เป็น `<` เพื่อกัน payload ปิด `<script>` ก่อนเวลา

---

## 13. ระบบ Login (Phase 2 — เสร็จ 2026-09-16)

### 13.1 โครงโฟลเดอร์ใหม่

โปรเจกต์ถูกจัดใหม่ให้ **โค้ดหลังบ้านอยู่นอกส่วนที่เว็บเข้าถึงได้** ซึ่งเป็นรากฐานด้านความปลอดภัยที่แก้ทีหลังยาก

    Project_FMK/
    ├── public/              ← เท่านี้ที่จะอัปโหลดขึ้น public_html
    │   ├── index.html       (generate จาก content.json)
    │   ├── logo-*.png/jpeg
    │   └── admin/
    │       ├── login.php    หน้าเข้าสู่ระบบ
    │       ├── index.php    แผงควบคุม
    │       ├── logout.php   ออกจากระบบ (POST + CSRF เท่านั้น)
    │       └── style.css
    ├── app/                 ← โค้ดแกนกลาง เข้าผ่าน URL ไม่ได้
    │   ├── bootstrap.php    autoload + security headers
    │   ├── Config.php       อ่านค่าตั้ง
    │   ├── Db.php           PDO, prepared statement เท่านั้น
    │   ├── Session.php      session ที่ตรวจฝั่งเซิร์ฟเวอร์
    │   ├── Csrf.php         token ต่อ session
    │   ├── Auth.php         ล็อกอิน / ตรวจสิทธิ์ / rate limit
    │   ├── Audit.php        ประวัติการใช้งาน
    │   └── helpers.php      escape, IP, security headers
    ├── config/
    │   ├── config.example.php   ตัวอย่าง (ไม่มีรหัส)
    │   └── config.local.php     ← มีรหัส · อยู่ใน .gitignore
    ├── db/001_init.sql
    ├── bin/migrate.php          รัน migration
    └── bin/create-user.php      สร้างบัญชีผู้ดูแล

**สำคัญตอน deploy:** อัปโหลดเฉพาะเนื้อใน `public/` ขึ้น `public_html` ส่วน `app/` `config/` `db/` `bin/` ต้องไปอยู่ **ระดับเหนือ `public_html`** ซึ่ง DirectAdmin เข้าถึงผ่านเว็บไม่ได้

### 13.2 ตารางที่เพิ่ม

| ตาราง | หน้าที่ |
|---|---|
| `users` | บัญชีผู้ดูแล — role `admin`/`editor`, status `active`/`disabled` |
| `sessions` | session ฝั่งเซิร์ฟเวอร์ เก็บ **sha256 ของ session id** ไม่ใช่ตัวจริง |
| `login_attempts` | บันทึกการลองเข้าระบบ ใช้จำกัดจำนวนครั้ง |
| `audit_log` | ใครทำอะไรเมื่อไร ตัดคีย์ที่ดูเหมือนความลับออกก่อนบันทึก |
| `schema_migrations` | เวอร์ชัน schema |

### 13.3 มาตรการความปลอดภัยที่ใส่ไว้

| มาตรการ | ทำยังไง |
|---|---|
| รหัสผ่าน | `password_hash()` — argon2id ถ้ามี ไม่งั้น bcrypt cost 12 · rehash อัตโนมัติเมื่อมาตรฐานเปลี่ยน |
| ตรวจสิทธิ์ | ทำฝั่งเซิร์ฟเวอร์ทุกครั้ง (`Auth::requireLogin()` / `requireRole()`) ไม่ใช่ซ่อนปุ่ม |
| Session fixation | `session_regenerate_id(true)` ทุกครั้งที่ล็อกอินสำเร็จ |
| ยกเลิก session ได้จริง | สถานะอยู่ในฐานข้อมูล ปิดบัญชีแล้วเตะออกทันทีในคำขอถัดไป |
| หมดอายุ | ไม่มีความเคลื่อนไหว 30 นาที หรือครบ 8 ชั่วโมงนับจากล็อกอิน |
| Cookie | `HttpOnly` + `SameSite=Strict` + `Secure` อัตโนมัติเมื่อเป็น HTTPS |
| CSRF | token ต่อ session เทียบด้วย `hash_equals` · ออก token ใหม่หลังล็อกอิน |
| ออกจากระบบ | รับเฉพาะ POST + CSRF — กันคนแปะลิงก์เตะเราออก |
| จำกัดการลองรหัส | ผิดเกิน 5 ครั้ง/อีเมล หรือ 15 ครั้ง/IP ใน 15 นาที → ล็อก 15 นาที |
| กันไล่เดาว่ามีบัญชีไหน | ข้อความผิดพลาดเหมือนกันหมด และยัง hash เทียบกับค่าหลอกเมื่อไม่พบบัญชี เพื่อให้เวลาตอบกลับเท่ากัน |
| Open redirect | พารามิเตอร์ `next` รับเฉพาะ path ที่ขึ้นต้นด้วย `/` และไม่ใช่ `//` |
| SQL injection | prepared statement ทั้งหมด ปิด `EMULATE_PREPARES` |
| XSS | escape ทุกจุดที่พิมพ์ข้อมูลผู้ใช้ ด้วย `e()` |
| Security headers | CSP, `X-Frame-Options: DENY`, `nosniff`, `Referrer-Policy`, ลบ `X-Powered-By` |
| แยกบัญชีจาก DirectAdmin | บัญชีหลังบ้านอยู่ในตาราง `users` ของเราเอง ไม่เกี่ยวกับบัญชีโฮสติ้ง |
| ไม่มีสมัครสมาชิก | สร้างบัญชีได้ทางคำสั่ง CLI เท่านั้น |

### 13.4 ผลทดสอบ (13/13 ผ่าน)

ยิงผ่าน HTTP จริง ทั้งเคสที่ควรผ่านและเคสที่ควรถูกปฏิเสธ:

| # | ทดสอบ | ผล |
|---|---|---|
| 1 | ยังไม่ล็อกอิน เข้า dashboard ไม่ได้ | ✅ 302 → login |
| 2 | ส่ง security headers ครบ | ✅ |
| 3 | cookie เป็น HttpOnly + SameSite=Strict | ✅ |
| 4 | POST โดยไม่มี CSRF token | ✅ ปฏิเสธ |
| 5 | CSRF token ปลอม | ✅ ปฏิเสธ |
| 6 | ข้อความผิดพลาดไม่บอกว่ามีบัญชีนั้นจริงไหม | ✅ เหมือนกันทั้งสองกรณี |
| 7 | ล็อกอินด้วยรหัสที่ถูกต้อง | ✅ |
| 8 | เปลี่ยน session id หลังล็อกอิน | ✅ |
| 9 | เข้า dashboard ได้ + แสดงบทบาทถูก | ✅ |
| 10 | logout ผ่าน GET ไม่ทำงาน | ✅ |
| 11 | ออกจากระบบแล้วเข้าไม่ได้อีก | ✅ |
| 12 | ล็อกหลังลองผิดหลายครั้ง | ✅ ล็อกที่ครั้งที่ 6 |
| 13 | เข้าถึง `app/` `config/` `db/` ผ่าน URL ไม่ได้ | ✅ 404 ทุกเส้นทาง รวม `../` และ `%2e%2e` |

### 13.5 คำสั่ง

    php bin/migrate.php                                    รัน migration
    php bin/create-user.php <อีเมล> <admin|editor> [ชื่อ]   สร้าง/แก้บัญชี (ถามรหัสผ่านแบบไม่โชว์)
    php bin/create-user.php <อีเมล> admin "ชื่อ" --generate สุ่มรหัสผ่านให้เลย

เข้าหลังบ้านที่ **http://localhost:8080/admin/login.php**

### 13.6 ยังไม่ได้ทำ

- หน้าจัดการผู้ใช้ผ่านเว็บ (ตอนนี้สร้างบัญชีได้ทาง CLI เท่านั้น) — ตั้งใจให้เป็นแบบนี้ไปก่อน
- บังคับเปลี่ยนรหัสผ่านครั้งแรก (คอลัมน์ `must_change_pw` เตรียมไว้แล้ว ยังไม่ได้ใช้)
- ลืมรหัสผ่าน / ส่งอีเมลรีเซ็ต
- 2FA
- ยังไม่ได้ทดสอบบน Apache จริง — ปัจจุบันทดสอบบน PHP built-in server เท่านั้น
  **ตอนขึ้นโฮสต์ต้องเพิ่ม `.htaccess` ป้องกันอีกชั้น** เพราะ document root ที่นั่นจะไม่เหมือนกันเป๊ะ

---

## 14. ระบบจัดการเนื้อหา (Phase 3 + ส่วนเผยแพร่ของ Phase 6 — เสร็จ 2026-09-16)

### 14.1 แนวคิด

เนื้อหาเก็บเป็นเอกสาร JSON ทั้งก้อนในฐานข้อมูล ไม่แตกเป็นตารางย่อย เพราะรูปทรงต้องตรงกับที่หน้าเว็บใช้อยู่แล้วเป๊ะ ๆ แตกแล้วประกอบกลับเสี่ยงเพี้ยน และเนื้อหาทั้งเว็บมีแค่ ~32 KB

**เส้นทางของข้อมูล:**

    หลังบ้าน → content_draft (ฉบับร่าง) → กดเผยแพร่ → public/index.html + content_versions (ประวัติ)

หน้าเว็บสาธารณะยังเป็น **static ไฟล์เดียว ไม่พึ่ง PHP หรือฐานข้อมูลเลย** ต่อให้หลังบ้านล่มทั้งระบบ เว็บที่ลูกค้าเห็นก็ยังทำงานปกติ

### 14.2 ทำอะไรได้บ้าง

| ความสามารถ | สถานะ |
|---|---|
| แก้ข้อความไทย/อังกฤษ ทุกส่วน แสดงคู่กันให้เทียบได้ | ✅ |
| เพิ่ม / ลบ / เรียงลำดับ รายการ (บริการ โครงการ บทความ สาขา ฯลฯ) | ✅ |
| ซ่อนรายการโดยไม่ต้องลบ | ✅ |
| แก้ข้อมูลบริษัทและช่องทางติดต่อ | ✅ |
| ดูตัวอย่างก่อนเผยแพร่ | ✅ |
| เผยแพร่พร้อมบันทึกช่วยจำ | ✅ |
| ประวัติการเผยแพร่ | ✅ |
| ย้อนกลับเวอร์ชันเก่า (เฉพาะ admin) | ✅ |
| ซ่อนทั้ง section | ❌ ยังทำไม่ได้ ต้องแก้ template |
| จัดการรูปภาพ | ❌ Phase 4 |

### 14.3 กติกาสำคัญที่บังคับไว้ในระบบ

- **การแก้ไม่ขึ้นเว็บทันที** ต้องกดเผยแพร่เสมอ
- **การเพิ่ม/ลบ/เรียง ทำกับสองภาษาพร้อมกันเสมอ** ถ้าทำแค่ภาษาเดียว โครงสร้างจะไม่ตรงกันและหน้าเว็บจะพังตอนสลับภาษา — ระบบตรวจและปฏิเสธการบันทึกที่ทำให้สองภาษาไม่ตรงกัน
- **การย้อนกลับไม่เผยแพร่ทันที** ดึงมาเป็นฉบับร่างก่อน เพื่อให้มีจังหวะตรวจ
- **รายการที่ซ่อนถูกตัดออกตั้งแต่ตอน generate** ไม่ได้ซ่อนด้วย CSS จึงไม่หลุดไปให้คนที่เปิดดู source เห็น
- **เขียนไฟล์แบบสลับ** เขียนลงไฟล์ชั่วคราวก่อนแล้วค่อย rename ไม่มีจังหวะที่ผู้ชมเจอไฟล์เขียนค้างครึ่งเดียว

### 14.4 build.js กับ Builder.php ต้องเหมือนกันเสมอ

มีตัว generate สองตัว — `build.js` (Node ใช้ตอน dev) และ `app/Builder.php` (PHP ใช้บนโฮสต์)
**ทั้งสองต้องให้ผลตรงกันทุกไบต์** มีเทสต์เทียบไว้แล้ว ถ้าแก้ตัวใดตัวหนึ่งต้องแก้อีกตัวด้วย
จุดที่ต้องตรงกัน: การตัดรายการที่ซ่อน · การลบคีย์ `visible` และคีย์ที่ขึ้นต้นด้วย `_` · การ escape `<` เป็น `\u003c`

### 14.5 บั๊กที่เจอระหว่างพัฒนา (บันทึกไว้กันพลาดซ้ำ)

**ตอนแรก `Builder::exportJson()` เขียน `content.json` ด้วยเนื้อหาที่ตัดรายการซ่อนออกแล้ว**
ผลคือพอนำเข้ากลับ รายการที่ซ่อนไว้จะ **หายถาวร** — เจอตอนรีเซ็ตฐานข้อมูลเพื่อทดสอบ แล้วบริการเหลือ 5 จาก 6

แก้โดยให้ `content.json` เก็บ **ฉบับเต็ม** รวมรายการที่ซ่อนและธง `visible`
ส่วนการตัดรายการซ่อนเป็นหน้าที่ของตอน render เท่านั้น และเพิ่ม `strip()` ใน `build.js` ให้ตรงกับ PHP

รายการที่หายไปกู้กลับมาได้จาก `index.original.html` ที่ไม่เคยถูกแตะ — **เป็นเหตุผลว่าทำไมต้องเก็บไฟล์ต้นฉบับไว้เสมอ**

### 14.6 ผลทดสอบ (21/21 ผ่าน)

ยิงผ่าน HTTP จริงทั้งหมด:

| กลุ่ม | ที่ทดสอบ |
|---|---|
| แก้เนื้อหา | โหลดข้อมูลจริงมาแสดง · แสดงบริการครบ 6 รายการ · บันทึกแล้วเก็บจริง |
| แยกร่าง/เผยแพร่ | แก้แล้วยังไม่ขึ้นเว็บ · ดูตัวอย่างเห็นฉบับร่าง · ตัวอย่างใส่ base href ให้รูปโหลดได้ |
| ซ่อนรายการ | สั่งซ่อนได้ · หลังเผยแพร่เหลือ 5 จาก 6 ทั้งสองภาษา · ไม่มีคีย์ `visible` หลุดไปหน้าเว็บ |
| เผยแพร่ | ระบบรู้ว่ามีงานค้าง · เผยแพร่สำเร็จ · เว็บจริงเปลี่ยนตาม |
| ย้อนกลับ | ประวัติแสดงเวอร์ชันเก่า · ย้อนกลับได้ · ย้อนแล้วเว็บจริงยังไม่เปลี่ยนจนกว่าจะกดเผยแพร่ |
| ความถูกต้อง | ปฏิเสธการแก้ที่ทำให้สองภาษาไม่ตรงกัน |
| สิทธิ์ | ไม่ล็อกอินเข้า content / publish / preview ไม่ได้ทั้งสามหน้า |

เพิ่มเติม: **PHP กับ Node สร้างหน้าเว็บออกมาเหมือนกันทุกไบต์** ทั้งกรณีปกติและกรณีมีรายการซ่อน
และ `verify.js` เดิมยังผ่านครบ 4 ชั้น — ยืนยันว่าหน้าเว็บสาธารณะยังเหมือนต้นฉบับเป๊ะ

### 14.7 คำสั่งเพิ่มเติม

    php bin/import-content.php            นำเข้าเนื้อหาจาก content.json เข้าฐานข้อมูล
    php bin/import-content.php --force    ล้างฉบับร่างแล้วนำเข้าใหม่ (งานที่ยังไม่เผยแพร่จะหาย)

หน้าหลังบ้าน:

    /admin/content.php    แก้เนื้อหา
    /admin/preview.php    ดูตัวอย่างฉบับร่าง
    /admin/publish.php    เผยแพร่ ประวัติ ย้อนกลับ

---

## 15. คลังรูปภาพ (Phase 4 — เสร็จ 2026-09-16)

### 15.1 หลักที่ใช้ออกแบบ

**คนใช้งานจริงคือคนทั่วไป ไม่ใช่ช่างเทคนิค** จึงยึดว่าผู้ใช้ไม่ควรต้องรู้เรื่องขนาดไฟล์ ความละเอียด หรือชนิดไฟล์ ถ่ายจากมือถือมาแล้วลากวางได้เลย ระบบจัดการที่เหลือให้เอง:

| ปัญหาที่คนทั่วไปมักเจอ | ระบบจัดการให้ยังไง |
|---|---|
| รูปจากมือถือตะแคง | หมุนให้ตั้งตรงอัตโนมัติตาม EXIF |
| รูปใหญ่มาก เว็บโหลดช้า | ย่อให้ด้านยาวสุดไม่เกิน 2400px ให้เอง พร้อมบอกว่าย่อจากเท่าไรเป็นเท่าไร |
| ไม่รู้ว่าต้องไฟล์อะไร | รองรับ JPG, PNG, WebP · ถ้าผิดชนิดจะบอกเป็นภาษาไทยว่าต้องใช้อะไร |
| รูปมือถือมีพิกัด GPS ติดมา | ล้าง metadata ทิ้งทั้งหมดตอนวาดรูปใหม่ |
| ลบรูปที่ใช้อยู่ แล้วเว็บรูปหาย | **ลบไม่ได้** ระบบบอกว่ารูปถูกใช้ที่ไหน ต้องเอาออกจากจุดนั้นก่อน |
| ต้องกรอกคำบรรยายซ้ำทุกจุดที่ใช้รูป | กรอกครั้งเดียวที่คลัง ระบบเติมให้ทุกจุดอัตโนมัติ ทั้งไทยและอังกฤษ |

### 15.2 หน้าตาการใช้งาน

**หน้าคลังรูปภาพ** — ลากรูปมาวางในกรอบใหญ่ หรือคลิกเลือกไฟล์ (เลือกทีละหลายรูปได้)
ระหว่างอัปโหลดมีข้อความบอกสถานะทีละไฟล์ เสร็จแล้วการ์ดรูปโผล่ขึ้นมาทันทีโดยไม่ต้องโหลดหน้าใหม่
แต่ละการ์ดบอก ชื่อไฟล์ · ขนาด · **ใช้อยู่ที่ไหน** และมีช่องกรอกคำบรรยายไทย/อังกฤษ

**หน้าแก้เนื้อหา** — ช่องรูปไม่ให้พิมพ์ที่อยู่ไฟล์เอง แต่เป็นปุ่ม "เลือกรูปจากคลัง"
กดแล้วเปิดหน้าต่างแสดงรูปทั้งหมดเป็นตาราง คลิกรูปเดียวจบ พร้อมภาพตัวอย่างให้เห็นทันที

### 15.3 จุดที่ใส่รูปได้

โครงเว็บเดิมออกแบบไว้รอรูปอยู่แล้ว มีข้อความ `PLACEHOLDER` คาไว้ 5 จุด — Phase 4 เข้าไปเติมให้ใช้ได้จริง:

| จุด | ช่องในข้อมูล |
|---|---|
| แบนเนอร์หลักหน้าแรก | `hero.image` |
| ภาพในส่วนเกี่ยวกับเรา | `about.image` |
| การ์ดผลงาน (ทุกใบ) | `projects.items[].image` |
| รูปบทความ (ทุกใบ) | `knowledge.items[].image` |
| รูปบริการ (ทุกใบ) | `solutions.items[].image` |
| โลโก้แถบบนและเมนู | `company.logo` |
| ตราสัญลักษณ์ท้ายเว็บ | `company.logoSeal` |

**ถ้ายังไม่ได้เลือกรูป หน้าเว็บจะแสดงเหมือนเดิมทุกตัวอักษร** — ยืนยันด้วย `verify.js` ที่ยังผ่านครบ 4 ชั้น
โลโก้ถอยกลับไปใช้ไฟล์เดิม (`logo-monogram.png` / `logo-seal.jpeg`) ถ้าไม่ได้ตั้งค่า

### 15.4 ความปลอดภัยของการอัปโหลด

| มาตรการ | ทำยังไง |
|---|---|
| ไม่เชื่อนามสกุลและ Content-Type | ตัดสินจาก `getimagesize()` ที่อ่านตัวไฟล์จริงเท่านั้น |
| โค้ดที่แอบฝังในรูป | ทุกไฟล์ถูก **วาดใหม่ผ่าน GD** ไม่ได้คัดลอกของเดิม โค้ดที่ฝังมาจึงไม่รอดถึงดิสก์ |
| ชื่อไฟล์อันตราย | ตั้งชื่อใหม่เป็นเลขสุ่ม 32 หลัก ไม่ใช้ชื่อจากผู้ใช้เป็นชื่อไฟล์เลย |
| โฟลเดอร์รูปถูกใช้รันสคริปต์ | วาง `.htaccess` ปิด ExecCGI และบังคับให้ `.php` เป็น text/plain |
| รูปความละเอียดสูงจนเซิร์ฟเวอร์ล่ม | ปฏิเสธถ้าเกิน 50 ล้านพิกเซล |
| ไฟล์ใหญ่เกิน | จำกัด 16 MB พร้อมข้อความบอกขนาดจริงที่ส่งมา |

### 15.5 ผลทดสอบ (22/22 ผ่าน)

| กลุ่ม | ที่ทดสอบ |
|---|---|
| อัปโหลด | รูป JPG ปกติ · รูป 4000×3000 ถูกย่อเหลือ 2400×1800 อัตโนมัติ |
| ปฏิเสธไฟล์อันตราย | ไฟล์ PHP เปลี่ยนนามสกุลเป็น .jpg · ไฟล์ที่ไม่ใช่รูป · **รูปจริงที่ต่อท้ายด้วยโค้ด PHP → โค้ดถูกล้างทิ้ง** |
| คำบรรยาย | บันทึกได้ · ถูกเติมให้ทุกจุดที่ใช้รูปอัตโนมัติ · ภาษาไทยได้คำบรรยายไทย ภาษาอังกฤษได้ของอังกฤษ |
| ใช้งานในเนื้อหา | ปุ่มเลือกรูปแสดงถูกจุด · หน้าต่างเลือกรูปมีรูปให้เลือก · ตั้งรูปแล้วบันทึกได้ |
| การ render จริง | **render หน้าเว็บออกมาจริงแล้วตรวจ** — รูปขึ้น · `alt` ถูกต้อง · ข้อความ PLACEHOLDER หายไป |
| กันลบผิด | ลบรูปที่ใช้อยู่ไม่ได้ · คลังบอกได้ว่ารูปถูกใช้ที่จุดไหน |
| เผยแพร่ | รูปขึ้นเว็บจริง · เปิดไฟล์รูปผ่าน URL ได้ และได้ content-type เป็น image |
| สิทธิ์ | ไม่ล็อกอินเข้าคลังไม่ได้ · อัปโหลดโดยไม่ล็อกอินไม่ได้ |

### 15.6 บั๊กที่เจอ (บันทึกไว้กันพลาดซ้ำ)

**บั๊กจริงที่เจอ:** `Media::usage()` เทียบ `$node === $id` แต่เนื้อหาเก็บเป็นที่อยู่ไฟล์เต็ม (`assets/media/<id>.jpg`) ไม่ใช่ id ล้วน
ผลคือระบบคิดว่าไม่มีรูปไหนถูกใช้เลย จึงยอมให้ลบรูปที่กำลังแสดงบนเว็บอยู่ → เว็บรูปหาย แก้เป็น `str_contains()`

**บั๊กที่ผู้ใช้เจอเอง — หน้าต่างเลือกรูปกดปิดไม่ได้:**
CSS เขียน `.picker{display:grid}` ซึ่ง **ทับ** `display:none` ที่มากับแอตทริบิวต์ `hidden` ของ HTML
ผลคือหน้าต่างเลือกรูปแสดงตลอดเวลาแม้ในหน้าที่ไม่มีช่องรูปเลย และปุ่มปิดทำงานจริง (ตั้ง `hidden=true`) แต่ไม่มีผลทางสายตา
โดนทั้ง `.picker` และ `.uploadlog` แก้ที่ต้นเหตุด้วยกฎรวม `[hidden]{display:none !important}` และเพิ่มเทสต์กันพลาดซ้ำ

**บทเรียน:** แอตทริบิวต์ `hidden` พึ่งไม่ได้ถ้าโปรเจกต์มี CSS ที่ตั้ง `display` ให้คลาสนั้น ต้องมีกฎรวมกันไว้เสมอ

**เรื่องวิธีทดสอบ:** ตอนแรกเขียนเทสต์ด้วยการ grep หาข้อความในหน้าเว็บ ซึ่งใช้ไม่ได้กับเว็บนี้ เพราะหน้าเว็บ render ด้วย JavaScript — โค้ดมีทั้งกิ่งที่ใส่รูปและกิ่ง PLACEHOLDER อยู่ในไฟล์เสมอ จะรู้ว่ากิ่งไหนทำงานต้อง **render ออกมาดูจริง ๆ** เทสต์จึงรันโค้ดหน้าเว็บในแซนด์บ็อกซ์แล้วตรวจที่ HTML ผลลัพธ์

### 15.7 ยังไม่ได้ทำ

- ครอบตัดรูป (crop) หรือเลือกจุดโฟกัส — ตอนนี้รูปถูก `object-fit: cover` ให้เต็มกรอบ ถ้าสัดส่วนไม่พอดีจะถูกตัดขอบอัตโนมัติ
- แทนที่รูปเดิมด้วยรูปใหม่โดยไม่ต้องไปเปลี่ยนทุกจุดที่ใช้
- แปลงเป็น WebP อัตโนมัติเพื่อลดขนาด
- รูปหลายขนาดสำหรับจอต่าง ๆ (`srcset`)
- `og:image` ยังชี้ `logo-full.jpeg` ที่ไม่มีอยู่จริง — ยังแก้ผ่านหลังบ้านไม่ได้ เพราะ meta อยู่ใน `<head>` แบบฝังตาย

---

## 16. ลิงก์เข้าหลังบ้านจากหน้าเว็บ (เสร็จ 2026-09-16)

### 16.1 ที่มา

ผู้ใช้ถามว่าเข้าหลังบ้านจากหน้าเว็บเลยได้ไหม จะได้ไม่ต้องจำที่อยู่

### 16.2 ข้อแลกเปลี่ยนที่ต้องรู้

| แบบ | ข้อดี | ข้อเสีย |
|---|---|---|
| มีลิงก์ท้ายเว็บ | หาเจอง่าย ไม่ต้องจำ URL | บอกคนทั้งโลกว่าหลังบ้านอยู่ตรงไหน บอตจะมาลองสุ่มรหัสมากขึ้น |
| ไม่มีลิงก์ | หลังบ้านเงียบกว่า บอตทั่วไปหาไม่เจอ | ต้องพิมพ์ URL เองหรือเก็บบุ๊กมาร์ก |

**สำคัญ:** การซ่อนลิงก์ **ไม่ใช่มาตรการความปลอดภัย** ความปลอดภัยจริงอยู่ที่การตรวจสิทธิ์ฝั่งเซิร์ฟเวอร์ การ hash รหัสผ่าน และการจำกัดจำนวนครั้งที่ลองผิด ซึ่งทำไว้ครบแล้วใน Phase 2

### 16.3 ที่ทำ

ทำเป็น **สวิตช์เปิด/ปิด** ให้ผู้ดูแลเลือกเอง อยู่ที่หน้า **ข้อมูลบริษัท** ในหลังบ้าน

- **ค่าเริ่มต้นคือปิด** — หน้าเว็บจึงเหมือนเดิมทุกตัวอักษรจนกว่าจะเปิดเอง
- เปิดแล้วจะมีลิงก์เล็ก ๆ ท้ายเว็บ แถวเดียวกับ Privacy Policy
- ข้อความแก้ได้ทั้งสองภาษา (ค่าเริ่มต้น "เข้าสู่ระบบผู้ดูแล" / "Staff Login")
- ลิงก์มี `rel="nofollow"` และเพิ่ม `robots.txt` ที่ `Disallow: /admin/` เพื่อไม่ให้ค้นเจอใน Google
- หน้า login มี `<meta name="robots" content="noindex, nofollow">` อยู่แล้ว

ระบบหลังบ้านรองรับช่องแบบ **เปิด/ปิด** เป็นครั้งแรก (เดิมมีแค่ข้อความกับรายการ)
เทคนิคสำคัญ: เบราว์เซอร์ไม่ส่งค่าของ checkbox มาเลยเมื่อไม่ได้ติ๊ก จึงต้องส่งรายชื่อช่องทั้งหมดมาด้วย (`b_all[]`) แล้วตีความว่าช่องที่ไม่มาคือปิด

### 16.4 ผลทดสอบ (7/7)

ปิดอยู่ → ไม่มีลิงก์ · เปิดแล้ว → มีลิงก์ทั้งสองภาษา ข้อความถูกภาษา มี `rel=nofollow` · สวิตช์ในหลังบ้านเปิดและปิดได้จริงผ่านหน้าเว็บ

---

## 17. ⚠️ เหตุการณ์ข้อมูลผู้ใช้หาย และวิธีป้องกัน (2026-09-16)

### 17.1 เกิดอะไรขึ้น

ผู้ดูแลอัปโหลดโลโก้และตั้งเป็นโลโก้เว็บเมื่อ 13:50 หลังจากนั้นมีการรันชุดทดสอบอัตโนมัติ
ชุดทดสอบ `cmstest.js` มีขั้นตอนทดสอบ "ย้อนเวอร์ชัน" ซึ่ง **สั่งย้อนฉบับร่างไปเป็นเวอร์ชันเก่าบนฐานข้อมูลจริง**
ผลคือข้อความทดสอบกลับมาทับเนื้อหาจริง และการตั้งค่าที่ผู้ดูแลทำไว้ถูกเขียนทับ

ความผิดพลาดซ้อน: สคริปต์กู้คืนที่เขียนขึ้นครั้งแรกไปอ่าน `content.json` เป็นต้นแบบ
แต่ `content.json` ถูกเขียนทับทุกครั้งที่กดเผยแพร่ จึงมีข้อความของชุดทดสอบปนอยู่ด้วย — "กู้" แล้วได้ข้อความทดสอบกลับมาเหมือนเดิม

### 17.2 รากของปัญหา

**ชุดทดสอบรันบนฐานข้อมูลเดียวกับที่ผู้ดูแลใช้งานจริง** ไม่ใช่แค่สคริปต์กู้คืนมีบั๊ก

### 17.3 สิ่งที่ทำเพื่อไม่ให้เกิดซ้ำ

| มาตรการ | รายละเอียด |
|---|---|
| `bin/snapshot.php` | สำรอง/กู้คืนสถานะทั้งหมด — ฉบับร่าง ประวัติ คลังรูป และไฟล์รูปจริง |
| `baseline.json` | เนื้อหาเว็บเดิม สกัดจาก `index.original.html` ที่ไม่เคยถูกแตะ **ไม่เปลี่ยนเลย** ใช้เป็นหลักอ้างอิงถาวร |
| ลบ `clean-test-data.php` | ตรรกะ "เดาว่าอะไรคือของเทสต์" เปราะเกินไป ใช้ snapshot ที่ชัดเจนกว่าแทน |

**กฎบังคับก่อนรันเทสต์ทุกครั้ง:**

    php bin/snapshot.php save user-work     ← ก่อนรันเทสต์
    ... รันเทสต์ ...
    php bin/snapshot.php restore user-work  ← หลังรันเทสต์

ยืนยันแล้วว่าใช้ได้จริง: รันเทสต์ทั้ง 3 ชุด (59 รายการ) จนข้อมูลเละ แล้วกู้คืน ได้โลโก้ เนื้อหา และคลังรูปกลับมาครบเป๊ะ

### 17.4 บทเรียนที่ต้องจำ

- **content.json ไม่ใช่ต้นฉบับ** เป็นไฟล์ส่งออกที่ถูกเขียนทับทุกครั้งที่เผยแพร่ ต้นฉบับคือ `index.original.html` และ `baseline.json`
- **เทสต์ที่แก้ข้อมูลต้องมีที่อยู่ของตัวเอง** หรืออย่างน้อยต้องสำรอง/กู้คืนรอบการรัน
- **ก่อนขึ้นเว็บจริง ห้ามรันชุดทดสอบเหล่านี้ชี้ไปที่ฐานข้อมูลบนโฮสต์เด็ดขาด**

---

## 18. การแก้ตามที่ผู้ใช้ทดลองใช้จริงแล้วสะดุด (2026-09-16)

จุดเหล่านี้ผู้ใช้เป็นคนพบเองจากการลองใช้ ชุดทดสอบอัตโนมัติจับไม่ได้ทั้งหมด
บันทึกไว้เพื่อให้เห็นว่าการทดสอบแบบไหนยังขาด

### 18.1 หน้าต่างเลือกรูปกดปิดไม่ได้

CSS `.picker{display:grid}` ทับ `display:none` ของแอตทริบิวต์ `hidden`
แก้ด้วยกฎรวม `[hidden]{display:none !important}` · **ชุดทดสอบไม่ประมวลผล CSS จึงจับไม่ได้**

### 18.2 โลโก้พื้นหลังโปร่งใสกลายเป็นดำ

สองบั๊กซ้อนกัน:
1. บันทึก PNG โดยไม่สั่ง `imagesavealpha()` → ชั้นความโปร่งใสถูกทิ้ง กระทบ **ไฟล์จริงบนหน้าเว็บ** ไม่ใช่แค่รูปย่อ
2. สร้างรูปย่อเป็น JPEG ซึ่งไม่มีชั้นความโปร่งใสตั้งแต่แรก

แก้แล้ว: บังคับ `imagealphablending(false)` + `imagesavealpha(true)` ก่อนบันทึก PNG/WebP ·
ล้างผืนผ้าใบให้โปร่งก่อนย่อรูป (ผืนผ้าใบใหม่ของ GD เป็นสีดำทึบ ถ้าไม่ล้างขอบจะมีคราบดำ) ·
**เปลี่ยนรูปย่อเป็น WebP** ซึ่งเก็บความโปร่งใสได้และเล็กกว่า JPEG

เพิ่มชุดทดสอบ `alphatest.js` (7 รายการ) อัปโหลด PNG โปร่งใสผ่าน HTTP จริงแล้วตรวจค่า alpha ของไฟล์ที่เก็บและรูปย่อ

**ข้อจำกัดที่ยังอยู่:** ระบบเก็บเฉพาะไฟล์ที่ประมวลผลแล้ว ไม่เก็บไฟล์ต้นฉบับ
รูปที่เสียไปก่อนแก้บั๊กจึงกู้ไม่ได้ ต้องอัปโหลดใหม่ (ยกเว้นโลโก้ของเว็บเดิมที่ยังมีไฟล์ต้นฉบับอยู่ในโปรเจกต์)

### 18.3 ออกจากระบบแล้วกลับหน้าเว็บไม่ได้

เดิมกดออกจากระบบแล้วค้างอยู่ที่หน้า login ไม่มีทางกลับไปหน้าเว็บ ต้องพิมพ์ URL เอง
แก้เป็น:
- หน้า login มีลิงก์ **← กลับไปหน้าเว็บไซต์**
- ออกจากระบบแล้วเด้งไป `login.php?bye=1` ซึ่งแสดงข้อความ **"ออกจากระบบเรียบร้อยแล้ว"** ให้แน่ใจว่ากดติด
- แถบบนของหลังบ้านเพิ่มปุ่ม **ดูหน้าเว็บ** เปิดแท็บใหม่ (`target="_blank"` + `rel="noopener"`) จะได้ไม่ทิ้งงานที่แก้ค้างไว้

ทดสอบแล้ว 9/9

### 18.4 บทเรียนรวม

**ชุดทดสอบที่มีตรวจเฉพาะตรรกะฝั่งเซิร์ฟเวอร์และ HTML ที่ render ออกมา**
เรื่องที่ยังต้องพึ่งคนเปิดดูจริง: การแสดงผลด้วย CSS · สีและความโปร่งใสของรูป · ความลื่นไหลของการใช้งาน
ก่อนขึ้นเว็บจริงควรมีคนไล่กดใช้งานจริงทุกหน้าอย่างน้อยหนึ่งรอบ

---

## 19. ปรับ UX/UI หลังบ้าน (2026-09-16)

เป้าหมาย: ให้ผู้ดูแลที่ไม่ใช่ช่างเทคนิคใช้งานได้โดยไม่ต้องมีคนสอน
ทำบนโครงเดิมทั้งหมด — PHP + MariaDB + โฮสติ้งเดิม ไม่เพิ่ม framework ไม่เพิ่มบริการรายเดือน

### 19.1 สิ่งที่ทำ

| # | งาน | ทำอย่างไร |
|---|---|---|
| 1 | เตือนเมื่อยังไม่ได้บันทึก | `beforeunload` + ดักการกดลิงก์ · ธงจะปลดเองตอนกดบันทึก |
| 2 | แถบคำสั่งลอยตาม | `position:sticky` มีสถานะ + ปุ่ม บันทึก / ดูตัวอย่าง / ไปหน้าเผยแพร่ |
| 3 | สถานะรวมทุกหน้า | แถบใต้เมนู บอก "เว็บเป็นเวอร์ชันล่าสุดแล้ว" หรือ "มีฉบับร่างที่ยังไม่ได้เผยแพร่" |
| 4 | ป้ายภาษาบนมือถือ | ทุกช่องมีป้าย **ภาษาอังกฤษ / ภาษาไทย** กำกับ แสดงเมื่อจอ ≤1000px |
| 5 | จัดกลุ่มเมนู | 8 กลุ่มตามงานที่คนคิดจะทำ ไม่ใช่ตามโครงสร้างข้อมูล |
| 6 | ซ่อนฟิลด์เทคนิค | ซ่อน `num`, `key` · เปลี่ยน `k` (ไอคอน) เป็น dropdown ชื่อไทย 17 แบบ |
| 7 | หน้าหลักเรียบขึ้น | เหลืองานหลัก 3 อย่าง ตัดคำว่า Phase และโมดูลที่ยังไม่เปิดออก |
| 8 | แถบเครื่องมือหน้าตัวอย่าง | กลับไปแก้ไข · ดูเว็บปัจจุบัน · ไปหน้าเผยแพร่ |

### 19.2 สิ่งที่ปรับปรุงเพิ่มระหว่างทาง

**เปลี่ยนมาใช้ POST แล้ว redirect (PRG)** — เดิมกดรีเฟรชหลังบันทึกจะบันทึกซ้ำ
และถ้าเพิ่งกดเพิ่มรายการแล้วรีเฟรช จะได้รายการซ้ำอีกใบ ตอนนี้ปิดช่องนั้นแล้ว
ข้อความแจ้งผลเก็บไว้ใน session เพื่อข้ามการ redirect มาแสดง

**เลขลำดับบนการ์ดเรียงให้อัตโนมัติ** — เดิมสลับลำดับรายการแล้วเลข 01/02/03 ไม่ตามไปด้วย
ต้องไล่แก้เอง ตอนนี้ระบบใส่ให้ตามลำดับที่แสดงจริงทุกครั้งที่บันทึก

**ไอคอนซิงก์สองภาษาอัตโนมัติ** — หลังบ้านแสดงตัวเลือกไอคอนช่องเดียว
ระบบคัดลอกไปฝั่งไทยให้ ไม่งั้นสลับภาษาแล้วจะเห็นไอคอนคนละแบบ

**`bin/unlock-login.php`** — ผู้ดูแลที่ลองรหัสผิดจนถูกล็อก ไม่ต้องรอ 15 นาที
(`--status` ดูว่าใครถูกล็อกอยู่) เจอปัญหานี้เองตอนรันชุดทดสอบซ้ำจนถูกนับว่า IP ลองผิดเกิน

### 19.3 ⚠️ ช่องโหว่ของ snapshot ที่เพิ่งอุด

`bin/snapshot.php` เดิมสำรองแค่ **ฐานข้อมูล** ไม่ได้สำรอง `content.json` และ `public/index.html`
ชุดทดสอบที่กดเผยแพร่จึงเขียนทับสองไฟล์นี้ และ restore ก็กู้กลับไม่ได้
ผลคือ `content.json` ค้างอยู่กับเนื้อหาของชุดทดสอบ ทั้งที่ฐานข้อมูลกู้กลับมาแล้ว

แก้แล้ว: snapshot เก็บทั้งสองไฟล์ · และตามให้ทันว่ารูปย่อเปลี่ยนเป็น WebP แล้ว (เดิมยังอ้าง `.jpg` จึงกู้รูปย่อไม่กลับ)

**บทเรียน:** เครื่องมือสำรองต้องครอบคลุมทุกที่ที่ระบบเขียนข้อมูล ไม่ใช่แค่ฐานข้อมูล

### 19.4 ทำงานได้แม้ปิด JavaScript

ทดสอบด้วย curl ซึ่งไม่รัน JavaScript เลย — **6/6 ผ่าน**
ฟอร์มเป็นฟอร์ม HTML ธรรมดา · ปุ่มทุกปุ่มเป็น `type="submit"` จริง · สถานะการบันทึกมาจากฝั่งเซิร์ฟเวอร์ ·
ไม่มี `onclick` ฝังใน HTML (สอดคล้องกับ CSP) · แถบเครื่องมือหน้าตัวอย่างเป็น HTML ล้วน

JavaScript ทำหน้าที่แค่เพิ่มความสะดวก: เตือนเมื่อยังไม่บันทึก · เปลี่ยนป้ายสถานะแบบทันที · ลากวางอัปโหลด · หน้าต่างเลือกรูป

### 19.5 ผลทดสอบรวม

| ชุด | ผล |
|---|---|
| `verify.js` — หน้าเว็บสาธารณะยังเหมือนเว็บเดิม | 4/4 |
| `authtest.js` — เข้าสู่ระบบและความปลอดภัย | 13/13 |
| `cmstest.js` — แก้เนื้อหา เผยแพร่ ย้อนเวอร์ชัน | 21/21 |
| `mediatest.js` — คลังรูปภาพ | 25/25 |
| `alphatest.js` — ความโปร่งใสของรูป | 7/7 |
| `uxtest.js` — งาน UX รอบนี้ | 34/34 |
| ทดสอบแบบปิด JavaScript | 6/6 |
| **PHP `Builder` = Node `build.js` ทุกไบต์** | ✅ 88,987 bytes ตรงกัน |

**รวม 110 รายการ ผ่านทั้งหมด**

ทุกครั้งที่รันชุดทดสอบ ใช้ `snapshot save` ก่อนและ `snapshot restore` หลัง — ยืนยันแล้วว่าเนื้อหาของผู้ดูแลไม่ถูกแตะ

---

## 20. แก้หน้าตาหน้าแก้เนื้อหา (2026-09-16, ผู้ใช้แจ้ง)

ผู้ใช้ส่งภาพหน้าจอมาพร้อมข้อความว่า "แก้ Ui ส่วนนี้หน่อย มันดูไม่สวย"

### 20.1 บั๊ก — แถบคำสั่งลอยทับแถบเมนู

ในภาพหน้าจอ แถบคำสั่งสีขาวลอยคร่อมแถบเมนูสีกรมท่าอยู่กลางอากาศ

**สาเหตุ:** มีของสองชิ้นจอง "ขอบบนของจอ" พร้อมกัน

| องค์ประกอบ | เดิม |
|---|---|
| `.topbar` (แถบเมนู) | `position:sticky; top:0; z-index:10` |
| `.actionbar` (แถบคำสั่ง) | `position:sticky; top:0; z-index:20` |

ทั้งคู่เกาะที่ `top:0` ตัวที่ `z-index` สูงกว่าจึงวาดทับอีกตัว

**ทางแก้ที่เลือก:** เปลี่ยน `.topbar` เป็น `position:relative`
แถบคำสั่งจึงเกาะขอบบนได้ตัวเดียว ไม่ต้องเดาความสูงของแถบเมนู

> พิจารณาทางเลือกอื่นแล้วไม่เอา: การตั้ง `.actionbar{top:94px}` ให้พ้นแถบเมนู
> ต้องฝังตัวเลขความสูงไว้ในโค้ด ซึ่งจะผิดทันทีเมื่อแถบเมนูตัดบรรทัดบนจอแคบ
> หรือเมื่อแถบสถานะไม่แสดง

ผลข้างเคียงที่ยอมรับ: แถบเมนูกับแถบสถานะเลื่อนหายไปตอนเลื่อนลง
ไม่กระทบการใช้งานเพราะแถบคำสั่งมีปุ่ม "ไปหน้าเผยแพร่" อยู่แล้ว

### 20.2 ช่องเลือกรูปกินพื้นที่เกินจำเป็น

เดิมเป็นกล่องเปล่าอัตราส่วน 16:9 กว้าง 340px วางทับปุ่ม สูงเกือบ 250px ต่อหนึ่งช่อง
หน้าที่มีหลายรายการจึงต้องเลื่อนยาวมากกว่าจะเจอช่องถัดไป

เปลี่ยนเป็นวางรูปตัวอย่างขนาด 132px อัตราส่วน 4:3 ไว้ข้างปุ่มในแถวเดียวกัน
จอแคบกว่า 560px จึงสลับมาเรียงลงมา

### 20.3 ไฟล์ที่แก้

| ไฟล์ | เปลี่ยนอะไร |
|---|---|
| `public/admin/style.css` | `.topbar` ไม่ sticky · `.sidenav` เว้นขอบ 14px แทน 78px · `.imgslot` เป็น flex · `.actionbar` มีเงาบาง ๆ |
| `public/admin/content.php` | ห่อปุ่มกับคำอธิบายไว้ใน `.imgbody` · ปุ่มมี `data-pick-label` |
| `public/admin/app.js` | เลือกรูปแล้วเปลี่ยนข้อความปุ่มเป็น "เปลี่ยนรูป" |

### 20.4 ชุดทดสอบใหม่ — `uitest.js` (15 รายการ)

บั๊กรอบนี้เป็นบั๊กเชิงการวางตำแหน่งของ CSS ซึ่งชุดทดสอบเดิมจับไม่ได้เลย
เพราะทุกตัวดู HTML อย่างเดียว ไม่ได้ประมวลผล CSS (เป็นบทเรียนเดียวกับบั๊ก
"กดปิดหน้าต่างเลือกรูปไม่ได้" ในข้อ 18)

`uitest.js` จึงอ่านกฎ CSS ตรง ๆ แล้วตรวจเงื่อนไขที่ทำให้เกิดการทับซ้อน:
**ห้ามมีมากกว่าหนึ่งองค์ประกอบที่เป็น `sticky` ที่ `top:0`**

ยืนยันแล้วว่าชุดทดสอบจับบั๊กได้จริง — ลองเปลี่ยน `.topbar` กลับเป็น `sticky`
แล้วรันใหม่ ได้ FAIL ตรงข้อที่ต้องการ

### 20.5 ผลทดสอบ

| ชุด | ผล |
|---|---|
| `verify.js` | 4 / 4 |
| `uitest.js` (ใหม่) | 15 / 15 |
| `uxtest.js` | 34 / 34 |
| `authtest.js` | 13 / 13 |
| `cmstest.js` | 21 / 21 |
| `mediatest.js` | 25 / 25 |
| `alphatest.js` | 7 / 7 |
| ปิด JavaScript | 6 / 6 |
| **PHP `Builder` = Node `build.js`** | ✅ 88,323 bytes ตรงกันทุกไบต์ |

**รวม 125 รายการ ผ่านทั้งหมด**

`snapshot save ui-fix-before` ก่อนรัน และ `restore` หลังรัน
ยืนยันสถานะหลังกู้คืน: โซลูชัน 6 · ผลงาน 3 · บทความ 3 · เหตุผล 4 · รูป 3 · เวอร์ชัน 13 · หัวเรื่องไทยครบ

บัญชีทดสอบชั่วคราวที่สร้างเพื่อรันชุดทดสอบถูกลบทิ้งแล้วหลังรันเสร็จ

---

## 21. แก้ตาม code review (2026-09-16)

### 21.1 ช่องทางติดต่อ (Social) — key ว่างได้และพิมพ์ผิดได้

**ปัญหา:** ข้อ 20 ซ่อนฟิลด์ `key` ไว้เพราะถือว่าเป็นฟิลด์เทคนิค
แต่สำหรับช่องทางติดต่อ `key` คือ "ช่องทางไหน" ซึ่งเป็นข้อมูลที่ผู้ดูแลต้องเลือกเอง
พอซ่อนไว้ รายการที่เพิ่มใหม่จึงได้ `key` ว่าง แล้ว `SOCIAL_PATHS[s.key]` คืนค่าว่าง
ได้ไอคอนเปล่า ๆ ที่กดแล้วไม่ไปไหนขึ้นท้ายเว็บ โดยไม่มีอะไรเตือนเลย

**แก้:**

| จุด | ทำอะไร |
|---|---|
| `Sections::socialOptions()` | รายชื่อช่องทางที่รองรับ — Facebook · YouTube · LINE · WhatsApp |
| `Sections::isHiddenField($key, $path)` | ไม่ซ่อน `key` เมื่อ path ตรงกับ `company.social.N.key` |
| `content.php` → `renderSocialKey()` | แสดงเป็น dropdown ชื่อไทย และปิดตัวเลือกที่รายการอื่นใช้ไปแล้ว |
| `content.php` → `addSocial()` | เพิ่มรายการใหม่ได้ประเภทที่ยังว่างอัตโนมัติ ถ้าครบทุกประเภทแล้วก็ไม่ให้เพิ่ม |
| `Content::validateSocial()` | ปฏิเสธ key ว่าง / ไม่รองรับ / ซ้ำ ที่ฝั่งเซิร์ฟเวอร์ |

`disabled` บน `<option>` เป็นของ HTML เอง จึงกันการเลือกซ้ำได้แม้ปิด JavaScript
และยังตรวจซ้ำที่เซิร์ฟเวอร์อีกชั้น เพราะ dropdown กันได้แค่คนที่ใช้หน้าจอตามปกติ

**การตัดสินใจที่ต้องบันทึกไว้:** รายการที่เพิ่มใหม่ถูกตั้งเป็น **ซ่อน** ไว้ก่อน
และกฎ "ชื่อที่แสดงกับลิงก์ห้ามว่าง" บังคับเฉพาะรายการที่เปิดแสดงอยู่

เหตุผล: ถ้าบังคับทุกรายการ จะเพิ่มรายการใหม่ไม่ได้เลย เพราะตัว `add` เองก็ต้อง
เรียก `saveDraft()` ซึ่งจะไม่ผ่านการตรวจตั้งแต่แรก (ลิงก์ยังว่างอยู่) กลายเป็นทางตัน
และถ้าปล่อยให้แสดงทันที ท้ายเว็บจะมีไอคอนที่กดแล้วไม่ไปไหน

### 21.2 คำเตือนออกจากหน้าซ้ำสองรอบ

**ปัญหา:** ตัวจับคลิกลิงก์ถาม `confirm()` หนึ่งรอบ พอผู้ใช้ตอบตกลง
เบราว์เซอร์ก็ยิง `beforeunload` ต่อ แล้วถามซ้ำอีกรอบ

**แก้:** เปลี่ยนชื่อธง `submitting` เป็น `navigating` ให้ครอบคลุมทั้ง
"กดบันทึก" และ "ยืนยันว่าจะออกแล้ว" แล้วตั้งธงทันทีหลังผู้ใช้ตอบตกลง

เพิ่มเงื่อนไขที่ไม่ควรเตือนด้วย เพราะไม่ได้ออกจากหน้านี้จริง:
คลิกกลาง · Ctrl/Cmd/Shift/Alt + คลิก · `target` ที่ไม่ใช่ `_self` ·
`download` · `#...` · `javascript:` / `mailto:` / `tel:`

และดัก `pageshow` ที่ `persisted` เพื่อล้างธงเมื่อผู้ใช้กดย้อนกลับ
ไม่งั้นครั้งถัดไปจะออกจากหน้าได้เงียบ ๆ โดยไม่เตือน

### 21.3 การเข้าถึงหน้าต่างเลือกรูป

เพิ่ม: จำปุ่มที่เปิด · คืน focus ให้ปุ่มนั้นตอนปิด · ขัง Tab/Shift+Tab ไว้ในหน้าต่าง ·
ไม่มีรูปในคลังก็ส่ง focus ไปปุ่มปิด · คลิกพื้นที่มืดปิดได้ ·
เปลี่ยน `aria-label` เป็น `aria-labelledby` ผูกกับหัวเรื่องจริง · `tabindex="-1"` บนกล่อง ·
คืนค่า `overflow` เดิมแทนการล้างทิ้ง

### 21.4 ชุดทดสอบใหม่ — รัน app.js จริงบน DOM จำลอง

งานข้อ 21.2 กับ 21.3 เป็นตรรกะฝั่งเบราว์เซอร์ล้วน ๆ ทดสอบผ่าน HTTP ไม่ได้
และโปรเจกต์นี้ห้ามเพิ่ม dependency จึงเขียน **DOM จำลองขนาดเล็กเอง** (`domstub.js`)
แล้วรัน `public/admin/app.js` ตัวจริงบนนั้น ยิงเหตุการณ์ click / keydown / submit จริง

ยืนยันแล้วว่าชุดทดสอบจับบั๊กได้จริง — ทำสำเนา app.js ที่ถอดการแก้ออก
แล้วรันใหม่ ได้ FAIL 5 ข้อตรงที่ควรพัง (เตือนซ้ำ 1 ข้อ · focus ไม่คืน 4 ข้อ)

> ข้อจำกัดที่ต้องรู้: DOM จำลองไม่มี CSS ไม่มี layout ไม่มี focus จริงของเบราว์เซอร์
> ทดสอบได้แค่ตรรกะของตัวจัดการเหตุการณ์ ไม่ได้แทนการกดจริงบนเบราว์เซอร์

### 21.5 ผลทดสอบ

| ชุด | ผล |
|---|---|
| `node --check public/admin/app.js` | ผ่าน |
| PHP lint (`app/` 12 ไฟล์ + `public/admin/` 8 ไฟล์) | ผ่านทั้ง 20 ไฟล์ |
| `node verify.js` | 4 / 4 |
| `socialtest.js` (ใหม่) | 25 / 25 |
| `jstest.js` บน DOM จำลอง (ใหม่) | 44 / 44 |
| `uitest.js` | 15 / 15 |
| `uxtest.js` | 35 / 35 |
| `authtest.js` | 13 / 13 |
| `cmstest.js` | 21 / 21 |
| `mediatest.js` | 25 / 25 |
| `alphatest.js` | 7 / 7 |
| ปิด JavaScript | 8 / 8 |
| **PHP `Builder` = Node `build.js`** | ✅ 88,987 bytes ตรงกันทุกไบต์ |

**รวม 193 รายการ ผ่านทั้งหมด**

ตรวจเพิ่ม: **ทั้ง 13 เวอร์ชันที่เผยแพร่ไว้ยังผ่านกฎใหม่** จึงย้อนกลับได้ตามปกติ
`snapshot save before-review-fixes` ก่อน และ `restore` หลัง —
ยืนยันสถานะหลังกู้คืน: ช่องทางติดต่อครบ 4 (facebook · youtube · line · whatsapp) แสดงทั้งหมด
ลิงก์ LINE เดิมยังอยู่ · โซลูชัน 6 · ผลงาน 3 · บทความ 3 · รูป 3 · เวอร์ชัน 13

---

## 22. จัดการผู้ใช้งาน (2026-09-16)

เดิมสร้างและแก้บัญชีได้ทางคำสั่ง CLI อย่างเดียว ตอนนี้ทำผ่านหน้าเว็บได้
**ไม่ต้องแก้ฐานข้อมูลเลย** — ตาราง `users` มีคอลัมน์ที่ต้องใช้ครบอยู่แล้วตั้งแต่ Phase 2
(`role`, `status`, `must_change_pw`) และตาราง `sessions` ก็เพิกถอนได้อยู่แล้ว

### 22.1 ไฟล์

| ไฟล์ | หน้าที่ |
|---|---|
| `app/Users.php` (ใหม่) | กฎทั้งหมดรวมไว้ที่เดียว หน้าเว็บกับ CLI ใช้ชุดเดียวกัน |
| `public/admin/users.php` (ใหม่) | รายชื่อ · สร้าง · แก้สิทธิ์/สถานะ · ตั้งรหัสใหม่ · เตะออก · ปลดล็อก · ลบ |
| `public/admin/account.php` (ใหม่) | บัญชีของฉัน — เปลี่ยนรหัสผ่าน ชื่อที่แสดง ดูเครื่องที่ล็อกอินอยู่ |
| `app/Auth.php` | `requireLogin()` บังคับเปลี่ยนรหัสตั้งต้นก่อนใช้งานหน้าอื่น |
| `app/Session.php` | เพิ่ม `currentHash()` เพื่อเตะ session อื่นแต่เก็บเครื่องนี้ไว้ |
| `app/Audit.php` | ย้าย `actionLabel()` มาเป็น `Audit::label()` ใช้ร่วมกันหลายหน้า |
| `public/admin/app.js` | ย้ายการถามยืนยันไปที่ระดับเอกสาร ปุ่มในหน้าผู้ใช้จึงถามได้ |
| `bin/create-user.php` | ใช้กฎรหัสผ่านจาก `Users` แทนที่จะเขียนซ้ำ |

### 22.2 กฎที่ยึด

**บทบาท** — `admin` ทำได้ทุกอย่างรวมถึงจัดการผู้ใช้ · `editor` แก้เนื้อหาและเผยแพร่ได้
แต่เข้าหน้าผู้ใช้และดูประวัติทั้งระบบไม่ได้ (บังคับที่เซิร์ฟเวอร์ด้วย `requireRole`
ไม่ใช่แค่ซ่อนเมนู — มีเทสต์ยิง POST ตรง ๆ ยืนยันว่าได้ 403)

**กันไม่ให้ระบบไม่มีผู้ดูแล** — ระบบนี้ไม่มีอีเมลกู้รหัส ถ้าไม่มีผู้ดูแลเหลือ
ทางแก้เดียวคือเข้าไปแก้ฐานข้อมูลตรง ๆ จึงกันไว้สามชั้น:

1. ลดสิทธิ์ ปิดบัญชี หรือลบ **ผู้ดูแลที่ใช้งานอยู่คนสุดท้าย** ไม่ได้
2. เปลี่ยนสิทธิ์หรือปิดบัญชี **ตัวเอง** ไม่ได้ (ต้องให้ผู้ดูแลอีกคนทำให้)
3. ลบบัญชีตัวเองไม่ได้

**รหัสผ่าน** — เน้นความยาว (อย่างน้อย 12) ไม่บังคับตัวใหญ่/อักขระพิเศษ
เพราะกฎแบบนั้นทำให้คนตั้ง `Password1!` ซึ่งเดาง่ายกว่าวลียาว ๆ
ปฏิเสธรหัสที่มีชื่ออีเมลอยู่ข้างใน และรหัสที่ติดรายการที่เดาง่าย

**must_change_pw** — รหัสที่ผู้ดูแลตั้งให้ผ่านมือคนอื่นมาแล้ว อาจอยู่ในแชตหรือกระดาษโน้ต
เจ้าของจึงถูกบังคับเปลี่ยนก่อนใช้งานหน้าอื่น ดักที่ `Auth::requireLogin()` ที่เดียว
ซึ่งเป็นทางผ่านของทุกหน้า ไม่มีหน้าไหนหลุดรอด

**เพิกถอน session** — เปลี่ยนรหัสผ่าน (ทั้งของตัวเองและที่ผู้ดูแลตั้งให้) ต้องเตะเครื่องอื่นออก
ไม่งั้นคนที่ขโมย session ไปยังใช้ต่อได้ทั้งที่เจ้าของเปลี่ยนรหัสแล้ว
ปิดบัญชีและลบบัญชีก็เตะออกทันทีเช่นกัน

**ลบ vs ปิด** — หน้าเว็บแนะนำให้ปิดมากกว่าลบ เพราะลบแล้วชื่อในประวัติการแก้ไข
จะกลายเป็นขีด (`audit_log` ตั้ง `ON DELETE SET NULL`)

### 22.3 ที่ยังไม่มีโดยตั้งใจ

**ลืมรหัสผ่านด้วยตัวเอง** — ต้องส่งอีเมล ซึ่งต้องใช้บริการภายนอกที่มีค่าใช้จ่าย
ขัดกับข้อกำหนดของโปรเจกต์ ทางแก้คือให้ผู้ดูแลอีกคนกดตั้งรหัสใหม่ให้
หน้า login เขียนบอกไว้แล้ว **จึงควรมีผู้ดูแลระบบอย่างน้อย 2 คนเสมอ**

**2FA** — ยังไม่มี

**จำกัดจำนวนครั้งที่กรอกรหัสเดิมผิดในหน้าเปลี่ยนรหัส** — ยังไม่มี
คนที่ยิงได้ต้องมี session ที่ใช้งานอยู่แล้ว ซึ่งทำอย่างอื่นได้อยู่ดี
ตัวป้องกันจริงคือ session หมดอายุเมื่อไม่มีความเคลื่อนไหว 30 นาที

### 22.4 ผลทดสอบ

| ชุด | ผล |
|---|---|
| `usertest.js` (ใหม่ · ผ่าน HTTP จริง) | 54 / 54 |
| `lastadmintest.php` (ใหม่ · กฎผู้ดูแลคนสุดท้าย) | 8 / 8 |
| `jsuser.js` (ใหม่ · ปุ่มยืนยันในหน้าผู้ใช้) | 4 / 4 |
| `jstest.js` | 44 / 44 |
| `socialtest.js` | 25 / 25 |
| `uitest.js` | 15 / 15 |
| `uxtest.js` | 35 / 35 |
| `authtest.js` | 13 / 13 |
| `cmstest.js` | 21 / 21 |
| `mediatest.js` | 25 / 25 |
| `alphatest.js` | 7 / 7 |
| ปิด JavaScript (หน้าผู้ใช้ + บัญชีของฉัน) | 7 / 7 |
| PHP lint 31 ไฟล์ · `node --check` · `node verify.js` | ผ่านทั้งหมด |
| **PHP `Builder` = Node `build.js`** | ✅ 88,987 bytes ตรงกันทุกไบต์ |

**รวม 262 รายการ ผ่านทั้งหมด**

> **วิธีทดสอบกฎผู้ดูแลคนสุดท้าย:** ทดสอบผ่านหน้าเว็บไม่ได้ เพราะเครื่องนี้มีบัญชีผู้ดูแลจริงอยู่
> จำนวนผู้ดูแลจึงไม่มีทางเหลือหนึ่งคน และจะไปแตะบัญชีจริงก็ไม่ได้
> จึงจำลองสถานการณ์ใน transaction แล้ว `rollback` ทิ้ง
> ตัวเทสต์เทียบ fingerprint ของตาราง `users` ก่อนและหลัง ยืนยันว่าไม่มีแถวไหนเปลี่ยน

**การป้องกันข้อมูลระหว่างทดสอบ:** `snapshot.php` ไม่ได้สำรองตาราง `users`
จึงเขียน `users-backup.php` เพิ่มไว้ในโฟลเดอร์ทดสอบ — `save` ก่อนรัน และ `check` หลังรัน
ยืนยันแล้วว่าบัญชีจริงเหมือนเดิมทุกคอลัมน์รวมถึง hash รหัสผ่าน และไม่มีบัญชีทดสอบค้าง

### 22.5 ยังไม่ได้ทดสอบบนเบราว์เซอร์จริง

ทุกอย่างในข้อนี้ทดสอบผ่าน HTTP และ DOM จำลอง ยังไม่มีใครกดจริง
จุดที่ต้องกดเองถึงจะรู้: ตารางผู้ใช้บนจอมือถือ (เลื่อนแนวนอน) ·
กล่องรหัสผ่านที่สุ่มให้ (คัดลอกง่ายไหม) · การบังคับเปลี่ยนรหัสครั้งแรก

---

## 23. แบบฟอร์มติดต่อของจริง (Phase 5 — เสร็จ 2026-09-16)

เดิมฟอร์มเป็น `setTimeout` ลูกค้ากรอกแล้วเห็น "บันทึกข้อมูลแล้ว" แต่**ข้อมูลหายทั้งหมด**
`README.md` ของ repo ต้นฉบับเขียนยืนยันไว้เองว่า *"The form is intentionally a demo — no data is sent."*

### 23.1 ไฟล์

| ไฟล์ | หน้าที่ |
|---|---|
| `db/004_messages.sql` (ใหม่) | ตาราง `contact_messages` และ `contact_blocked` |
| `app/Messages.php` (ใหม่) | ตรวจ · กันสแปม · บันทึก · อ่าน · ส่งออก CSV |
| `public/contact.php` (ใหม่) | ปลายทางที่ฟอร์มยิงเข้ามา (ไม่ต้องล็อกอิน) |
| `public/admin/messages.php` (ใหม่) | กล่องข้อความในหลังบ้าน |
| `bin/update-form-fields.php` (ใหม่) | เปลี่ยนข้อความฟอร์มจาก "ตัวอย่าง" เป็นของจริง |
| `template.html` | เปลี่ยน `setTimeout` เป็น `fetch('contact.php')` · เพิ่มช่องล่อบอท · กล่องแจ้งเตือนเมื่อส่งไม่สำเร็จ |
| `verify.js` | ปรับข้อ 3 และเพิ่มข้อ 5 |
| `_layout.php` · `index.php` · `style.css` · `Audit.php` · `config.example.php` | เมนู · การ์ดหน้าหลัก · สไตล์ · ชื่อการกระทำ · ค่าตั้งอีเมลแจ้งเตือน |

### 23.2 หลักที่ยึด

**ห้ามบอกลูกค้าว่าส่งสำเร็จทั้งที่ข้อมูลไม่ถึงเรา** — นี่คือความผิดพลาดของของเดิม
ถ้าเซิร์ฟเวอร์ปฏิเสธ เน็ตหลุด หรือตอบกลับมาอ่านไม่ออก ต้องขึ้นกล่องสีแดง
บอกตรง ๆ พร้อม**เบอร์โทรและอีเมลสำรอง** ไม่ใช่ปล่อยให้ลูกค้านั่งรอสายที่ไม่มีวันโทรกลับ

**ฐานข้อมูลคือแหล่งข้อมูลจริง** ทุกอย่างที่ทำหลังบันทึกสำเร็จ (เช่นส่งอีเมลแจ้งเตือน)
ถูกครอบด้วย try/catch ที่กลืนข้อผิดพลาด — อีเมลล่มได้ ข้อความของลูกค้าห้ามหาย

**หน้าเว็บยังเป็นไฟล์ static เหมือนเดิม** `contact.php` ถูกเรียกเฉพาะตอนกดส่ง
ถ้า PHP หรือฐานข้อมูลล่ม หน้าเว็บยังแสดงครบทุกส่วน แค่ส่งฟอร์มไม่ได้

### 23.3 กันสแปมโดยไม่เพิ่มบริการภายนอก

| วิธี | ทำงานอย่างไร |
|---|---|
| ช่องล่อบอท (honeypot) | ช่องชื่อ `website` ที่ `left:-9999px` + `tabindex="-1"` คนมองไม่เห็นและ Tab ไม่ถึง |
| กับดักเวลา | กรอกฟอร์ม 11 ช่องเสร็จใน 3 วินาทีคือเป็นไปไม่ได้สำหรับคน |
| จำกัดจำนวนครั้ง | 5 ครั้ง/ชั่วโมง และ 20 ครั้ง/วัน ต่อ IP |
| นับลิงก์ | ข้อความที่มีลิงก์เกิน 4 อันถือว่าเป็นสแปม |
| ตรวจ Origin | ยิงข้ามมาจากโดเมนอื่นถูกปฏิเสธ (ไม่ใช่ด่านหลัก ปลอมได้ แต่ตัดขยะได้ฟรี) |

> **จงใจตอบว่า "สำเร็จ" กับบอท** ทั้งที่ไม่ได้บันทึก — ถ้าบอกว่าโดนจับได้ คนเขียนบอทจะปรับวิธีจนผ่าน
> คำขอที่ถูกปฏิเสธถูกนับไว้ในตาราง `contact_blocked` และแสดงเป็นสถิติในหลังบ้าน

**ไม่ใช้ reCAPTCHA** เพราะเป็นบริการภายนอก ขัดกับข้อกำหนดของโปรเจกต์

### 23.4 อีเมลแจ้งเตือน — ปิดไว้เป็นค่าเริ่มต้น

ใส่ `contact_notify_email` ใน `config.local.php` เพื่อเปิด ใช้ `mail()` ของ PHP
ซึ่งใช้เมลเซิร์ฟเวอร์ของโฮสต์เอง ไม่ใช่บริการภายนอกและไม่มีค่าใช้จ่าย

**ปิดไว้ก่อนเพราะยังไม่ได้ทดสอบบนโฮสต์จริง** เปิดหลังทดสอบแล้วเท่านั้น
ต่อให้ส่งไม่สำเร็จ ข้อความก็ไม่หาย เพราะบันทึกลงฐานข้อมูลไปก่อนแล้ว

### 23.5 สิ่งที่ verify.js ไม่คุ้มครองแล้ว — ต้องรู้

ข้อ 3 เคยเทียบ HTML ที่ render ออกมาทั้งหมดกับเว็บเดิมแบบตัวอักษรต่อตัวอักษร
ตอนนี้ **หน้าต่างฟอร์มติดต่อไม่ถูกเทียบแล้ว** เพราะตั้งใจให้ต่างจากเดิม

สิ่งที่ยังคุ้มครองอยู่เต็มที่:
- เนื้อหาหน้าเว็บทั้งหน้า (ทุกส่วน ทั้ง EN และ TH) — เทียบตัวอักษรต่อตัวอักษร
- เมนูมือถือ — เทียบตัวอักษรต่อตัวอักษร

สิ่งที่ย้ายไปคุ้มครองด้วยข้อ 5 แทน:
- ช่องกรอกครบ 11 ช่องเท่าเดิม ไม่มีช่องไหนหายไป
- มีช่องล่อบอทและมองไม่เห็นจริง
- ไม่มีข้อความ "ตัวอย่าง / demo" เหลืออยู่
- template ส่งไปที่ `contact.php` จริงและไม่มีโค้ด demo ค้าง

### 23.6 ชุดทดสอบใหม่

`contacttest.js` (25) ยิงเข้า `contact.php` ผ่าน HTTP เหมือนเบราว์เซอร์จริง
`formjstest.js` (28) รันสคริปต์ของหน้าเว็บจริงใน Node แล้วกดส่งฟอร์ม

> **พิสูจน์แล้วว่าจับบั๊กได้** — ทำสำเนาหน้าเว็บที่แก้ให้ "บอกว่าสำเร็จเสมอ"
> แล้วรันใหม่ ได้ FAIL 9 ข้อตรงที่ควรพัง รวมถึงข้อ "บอกว่าสำเร็จทั้งที่ล้มเหลว — บั๊กร้ายแรง"

### 23.7 ผลทดสอบ

| ชุด | ผล |
|---|---|
| `contacttest.js` (ใหม่) | 25 / 25 |
| `formjstest.js` (ใหม่) | 28 / 28 |
| `verify.js` | 5 / 5 |
| `usertest.js` | 54 / 54 |
| `jstest.js` | 44 / 44 |
| `uxtest.js` | 36 / 36 |
| `socialtest.js` | 25 / 25 |
| `mediatest.js` | 25 / 25 |
| `cmstest.js` | 21 / 21 |
| `uitest.js` | 15 / 15 |
| `authtest.js` | 13 / 13 |
| `lastadmintest.php` | 8 / 8 |
| `alphatest.js` | 7 / 7 |
| `jsuser.js` | 4 / 4 |
| PHP lint 35 ไฟล์ · `node --check` | ผ่านทั้งหมด |
| **PHP `Builder` = Node `build.js`** | ✅ 92,862 bytes ตรงกันทุกไบต์ |

**รวม 310 รายการ ผ่านทั้งหมด**

ยืนยันสถานะหลังกู้คืน: โซลูชัน 6 · ผลงาน 3 · บทความ 3 · ช่องทางติดต่อ 4 · รูป 3 · เวอร์ชัน 13 ·
หัวเรื่องไทยครบ · บัญชี `dev@mcs.co.th` และ `admin@fmk.shop` ไม่ถูกแตะ (เทียบครบทุกคอลัมน์รวม hash)

### 23.8 ยังไม่ได้ทำ

- **ยังไม่ได้เผยแพร่** — ฉบับร่างมีข้อความฟอร์มชุดใหม่แล้ว แต่ต้องกดเผยแพร่เอง
- **ปิด JavaScript แล้วส่งฟอร์มไม่ได้** — แต่หน้าต่างฟอร์มก็เปิดไม่ได้อยู่แล้วตั้งแต่เว็บเดิม
  (ทั้งหน้าเว็บ render ด้วย JavaScript มาตั้งแต่ต้น) จึงไม่ใช่การถอยหลัง
- **ยังไม่ได้ทดสอบบนเบราว์เซอร์จริง**

---

## 24. ลืมรหัสผ่านทำอย่างไร (2026-09-16)

### 24.1 สามกรณี

| สถานการณ์ | ทางแก้ | มีตั้งแต่ |
|---|---|---|
| ผู้แก้ไขเนื้อหาลืมรหัส | ผู้ดูแลกด "ตั้งรหัสใหม่" ในหน้าผู้ใช้งาน | ข้อ 22 |
| ผู้ดูแลคนหนึ่งลืม แต่ยังมีผู้ดูแลอีกคน | ผู้ดูแลอีกคนตั้งรหัสใหม่ให้ | ข้อ 22 |
| **ผู้ดูแลลืมกันหมด ไม่มีใครเข้าได้เลย** | **`deploy/recover.php`** | ข้อนี้ |

กรณีที่สามเคยเป็นรูโหว่จริง — เดิมทางออกเดียวคือเข้าไปแก้ฐานข้อมูลด้วยมือผ่าน phpMyAdmin
ซึ่งผู้ดูแลทั่วไปทำไม่ได้ และ `bin/create-user.php` ก็ใช้ไม่ได้เพราะโฮสต์นี้ไม่มี SSH

### 24.2 `deploy/recover.php` ทำงานอย่างไร

**ไม่ได้อยู่ในชุดที่อัปขึ้นเว็บตามปกติ** — อยู่ในโฟลเดอร์ `deploy/` ต้องอัปเองเมื่อจำเป็น

1. อัปไฟล์ไปที่ `public_html/admin/recover.php` ผ่าน DirectAdmin File Manager
2. เปิดหน้านั้น ระบบสุ่มชื่อไฟล์ให้ เช่น `fmk-a1b2c3d4e5f6.txt`
3. ผู้ใช้ต้องไปสร้างไฟล์เปล่าชื่อนั้นในโฟลเดอร์เดียวกัน แล้วกดยืนยัน
4. ตั้งรหัสผ่านใหม่ (หรือสร้างบัญชีผู้ดูแลใหม่ ถ้ายังไม่มีเลย)
5. ลบ `recover.php` ทิ้ง

**ทำไมถึงปลอดภัย** — ต่อให้คนอื่นเจอไฟล์นี้ เขาก็ใช้ไม่ได้ เพราะต้องพิสูจน์ก่อนว่า
**เขียนไฟล์ลงเซิร์ฟเวอร์ได้** ซึ่งทำได้เฉพาะคนที่เข้า DirectAdmin หรือ FTP ได้
ซึ่งก็คือคนที่ควบคุมโฮสติ้งอยู่แล้ว (ถ้าเขามีสิทธิ์ระดับนั้น เขาทำอะไรกับเว็บก็ได้อยู่ดี)

ชั้นป้องกันอื่น: โจทย์สุ่มใหม่ทุกครั้งและใช้ได้ครั้งเดียว · ไฟล์พิสูจน์ถูกลบอัตโนมัติหลังใช้ ·
จำกัดจำนวนครั้ง · ต้องมี CSRF token · เพิกถอน session ทั้งหมดของบัญชีนั้น ·
บันทึกลงประวัติการกระทำว่า `user.recovered`

**แถบเตือนกันลืมลบ** — ถ้า `recover.php` ยังอยู่บนเว็บ หลังบ้านจะขึ้นแถบสีแดงทุกหน้า
บอกให้ลบทิ้ง ตราบใดที่ไฟล์ยังอยู่ (ตรวจด้วย `is_file()` ใน `_layout.php`)

### 24.3 แก้ปัญหาการติดตั้งครั้งแรกไปด้วย

ไฟล์เดียวกันสร้างบัญชีผู้ดูแล **คนแรก** ได้ ถ้ายังไม่มีบัญชีไหนเลยในฐานข้อมูล

เดิม DEPLOY.md เขียนว่า "ถ้าไม่มี SSH ต้องสร้าง hash ด้วยวิธีอื่นแล้ว insert ผ่าน phpMyAdmin"
ซึ่งใช้งานจริงแทบไม่ได้ ตอนนี้ขั้นตอนขึ้นเว็บไม่ต้องพึ่ง SSH อีกต่อไป

### 24.4 ที่ยังไม่มี

- **ลืมรหัสแล้วกู้เองผ่านอีเมล** ยังไม่มี เพราะต้องใช้บริการส่งอีเมลภายนอกที่มีค่าใช้จ่าย
- **ยังควรมีผู้ดูแลระบบอย่างน้อย 2 คน** — `recover.php` คือทางออกสุดท้าย ไม่ใช่ขั้นตอนปกติ

### 24.5 ผลทดสอบ

`recovertest.js` (ใหม่) **29 / 29** — ครอบคลุม:
ยิง POST ตรง ๆ โดยไม่พิสูจน์ตัวตน → ปฏิเสธและไม่สร้างบัญชี ·
ไฟล์ชื่ออื่นใช้แทนไม่ได้ · ตรวจรหัสผ่าน/อีเมล/CSRF ·
สร้างผู้ดูแลคนแรกได้ · ตั้งรหัสใหม่ให้บัญชีเดิมได้และดันขึ้นเป็น admin ·
โจทย์ใช้ซ้ำไม่ได้ · ไฟล์พิสูจน์ถูกลบอัตโนมัติ ·
แถบเตือนขึ้นขณะไฟล์ยังอยู่และหายไปเมื่อลบ · บันทึกลงประวัติ

PHP lint ผ่านทุกไฟล์ · `node verify.js` 5/5 ·
ตรวจแล้วบัญชีจริงทั้งสองไม่ถูกแตะ (เทียบครบทุกคอลัมน์รวม hash) และไม่มีไฟล์ตกค้างในโฟลเดอร์ admin

---

## 25. หน้า "ไม่มีสิทธิ์" อธิบายเหตุผล (2026-09-16, ผู้ใช้แจ้ง)

**อาการที่ผู้ใช้เจอ:** ล็อกอินด้วย `admin@fmk.shop` แล้วไม่เห็นบัญชี `dev@mcs.co.th`

**สาเหตุ:** ไม่ใช่บั๊ก — `admin@fmk.shop` มีสิทธิ์เป็น **ผู้แก้ไขเนื้อหา (editor)**
ไม่ใช่ผู้ดูแลระบบ จึงไม่เห็นเมนู "ผู้ใช้งาน" และเปิดหน้านั้นไม่ได้

สิ่งที่ทำให้สับสนคือ **ชื่ออีเมลมีคำว่า admin แต่สิทธิ์จริงเป็น editor**
ชื่ออีเมลไม่เกี่ยวกับสิทธิ์เลย ระบบดูจากคอลัมน์ `role` เท่านั้น

**สิ่งที่แก้:** หน้า 403 เดิมขึ้นแค่ "คุณไม่มีสิทธิ์เข้าหน้านี้" บนหน้าเปล่า
ไม่มีทางกลับ ไม่บอกเหตุผล ผู้ใช้จึงไม่รู้ว่าตัวเองทำอะไรผิด

ตอนนี้ `Auth::denyPage()` บอกครบ: บัญชีที่ใช้อยู่ · สิทธิ์ที่มี · สิทธิ์ที่ต้องใช้ ·
วิธีขอสิทธิ์ · และปุ่มกลับหน้าหลัก

> บทเรียน: การกันสิทธิ์ที่ถูกต้องยังทำให้ผู้ใช้งงได้ ถ้าไม่บอกว่าทำไม
> ชุดทดสอบเดิมตรวจแค่ว่า "ได้ 403" ซึ่งผ่านมาตลอด แต่ไม่ได้ตรวจว่าผู้ใช้เข้าใจไหม

**ทดสอบ:** `usertest.js` 54/54 · `uxtest.js` 36/36 · `cmstest.js` 21/21 ·
ตรวจหน้า 403 ด้วยบัญชี editor จริงแล้วเห็นข้อความครบถ้วน · บัญชีจริงไม่ถูกแตะ

---

## 26. กล่องยืนยันของระบบเอง (2026-09-16, ผู้ใช้แจ้ง)

ผู้ใช้ส่งภาพหน้าจอตอนกด "ตั้งรหัสใหม่" แล้วถามว่า "ทำไมไม่ขึ้นแบบปกติ"

### 26.1 สาเหตุ

ใช้ `confirm()` ของเบราว์เซอร์ ซึ่งขึ้นหัวว่า **"localhost:8080 says"**
ดูเหมือนคำเตือนของเบราว์เซอร์มากกว่าส่วนหนึ่งของระบบ และจัดรูปแบบอะไรไม่ได้เลย —
ปุ่ม "ลบบัญชีถาวร" กับ "บันทึก" หน้าตาเหมือนกันทุกประการ

### 26.2 สิ่งที่ทำ

สร้างกล่องยืนยันของระบบเองใน `app.js` (`askConfirm()`) ใช้กับ **การกระทำ** ทั้งหมด:
ลบบัญชี · ตั้งรหัสใหม่ · เตะออก · ลบข้อความ · ลบรายการเนื้อหา · ลบรูป

| จุด | เดิม | ใหม่ |
|---|---|---|
| หน้าตา | หน้าต่างเบราว์เซอร์ | กล่องในหน้า ใช้สีและฟอนต์ชุดเดียวกับหลังบ้าน |
| ปุ่มของการลบ | เหมือนปุ่มอื่น | สีแดงแยกออกมา (`danger`) |
| โฟกัสเริ่มต้น | ปุ่ม OK | **ปุ่มยกเลิก** — กด Enter รัว ๆ แล้วไม่เผลอลบ |
| การเข้าถึง | — | `role="alertdialog"` · `aria-modal` · ผูกหัวข้อ/คำอธิบาย · ขัง Tab · Escape · คืนโฟกัส |
| หัวข้อ | ไม่มี | ตั้งได้ด้วย `data-confirm-title` / `data-confirm-ok` |

**ยังใช้ของเบราว์เซอร์อยู่ 1 จุด** — คำเตือนตอนปิดแท็บทั้งที่ยังไม่บันทึก (`beforeunload`)
เบราว์เซอร์บังคับใช้หน้าต่างของตัวเองเสมอ แต่งไม่ได้

**เรื่องที่ต้องระวังในโค้ด:** `confirm()` หยุดรอคำตอบได้ แต่กล่องนี้ไม่หยุด
จึงต้อง `preventDefault()` ไว้ก่อนเสมอ แล้วค่อยส่งฟอร์มซ้ำด้วย `requestSubmit(ปุ่ม)`
เมื่อผู้ใช้ตอบยืนยัน — ต้องส่งตัวปุ่มไปด้วย ไม่งั้นเซิร์ฟเวอร์ไม่รู้ว่าสั่งอะไร
และต้องมีธงกันถามวนไม่จบ

ระหว่างทางได้รวมตัวขัง Tab ของกล่องยืนยันกับหน้าต่างเลือกรูปให้ใช้ฟังก์ชันเดียวกัน (`trapTab`)

### 26.3 กับดักที่เจอจากภาพหน้าจอเดียวกัน

ในภาพมีป้าย **"ต้องเปลี่ยนรหัส"** อยู่ที่ `dev@mcs.co.th` ไล่ประวัติแล้วพบว่า
เวลา 17:37:13 มีการกด **"ตั้งรหัสใหม่" ใส่บัญชีตัวเอง**

`Users::resetPassword()` สุ่มรหัสให้ แสดงครั้งเดียว แล้ว**เพิกถอน session ทุกเครื่องรวมถึงเครื่องที่กด**
ถ้าไม่ทันคัดลอกรหัส ก็เข้าบัญชีตัวเองไม่ได้อีก — เป็นกับดักที่ผมสร้างไว้เอง

**แก้:** แถวของตัวเองเปลี่ยนจากปุ่ม "ตั้งรหัสใหม่" เป็นลิงก์ **"เปลี่ยนรหัสของฉัน"**
ไปหน้าบัญชีของฉัน ซึ่งตั้งรหัสที่ตัวเองรู้และไม่หลุดจากระบบ
พร้อมกันที่ฝั่งเซิร์ฟเวอร์ด้วย (ยิง POST ตรง ๆ ก็ถูกปฏิเสธ)

### 26.4 ผลทดสอบ

`jsuser.js` เขียนใหม่ทั้งชุด **27 / 27** — ครอบคลุม: ไม่เรียก `confirm()` อีกแล้ว ·
ข้อความและการขึ้นบรรทัดตรงกับที่กำหนดบนปุ่ม · `role`/`aria` ถูกต้อง ·
โฟกัสเริ่มที่ปุ่มยกเลิก · ตอบยืนยันแล้วฟอร์มถูกส่งพร้อมค่าปุ่ม · ไม่ถามวนซ้ำ ·
ยกเลิก/Escape/คลิกนอกกล่อง · ขัง Tab · คืนโฟกัส

ชุดอื่นทั้งหมดยังผ่าน: `jstest` 44/44 (ยืนยันว่าการรวม `trapTab` ไม่ทำให้หน้าต่างเลือกรูปพัง) ·
`usertest` 54/54 · `formjstest` 28/28 · `contacttest` 25/25 · `socialtest` 25/25 ·
`mediatest` 25/25 · `uxtest` 36/36 · `cmstest` 21/21 · `uitest` 15/15 ·
`authtest` 13/13 · `lastadmintest` 8/8 · `alphatest` 7/7 · `verify.js` 5/5

ตรวจแล้วบัญชีจริงไม่ถูกแตะ ยกเว้นการเปลี่ยนสิทธิ์ `admin@fmk.shop` เป็นผู้ดูแลระบบตามที่ผู้ใช้สั่ง

---

## 27. ชุดไฟล์สำหรับขึ้นเว็บจริง (2026-09-17)

### ปัญหาที่ต้องแก้ก่อน

โฮสต์นี้เข้าได้แค่ **DirectAdmin File Manager กับ phpMyAdmin** ไม่มี SSH ไม่มี FTP
ซึ่งชนกับสมมติฐานเดิมของโปรเจกต์อยู่สามจุด และแต่ละจุดจะทำให้ติดตั้งไม่สำเร็จ

**หนึ่ง — ชื่อโฟลเดอร์เว็บ**
`Builder::writeIndex()` เขียนลง `<ราก>/public/index.html` ตายตัว
บนโฮสต์โฟลเดอร์นั้นชื่อ `public_html` และอยู่คนละชั้น
ผลคือกดเผยแพร่แล้วระบบขึ้นว่าสำเร็จ แต่หน้าเว็บจริงไม่เปลี่ยน — พลาดแบบเงียบ ๆ ซึ่งแย่ที่สุด

แก้ด้วย `Config::publicDir()` อ่านค่า `public_dir` จาก config
ถ้าตั้งเป็นโฟลเดอร์ที่ไม่มีอยู่จริง **ระบบโยน error ทันที** ไม่ปล่อยผ่าน
จุดที่เคยฝังชื่อ `public/` ไว้และเปลี่ยนมาใช้ตัวนี้: `Builder::writeIndex()`,
`Media::dir()`, `publish.php`, `bin/snapshot.php`

**สอง — migration รันไม่ได้**
`bin/migrate.php` และ `bin/import-content.php` เป็น CLI ล้วน
`bin/make-bundle.php` จึงสร้าง `dist/fmk-install.sql` ก้อนเดียวที่รวม 001–004
ตามลำดับ + `schema_migrations` + เนื้อหาตั้งต้นใน `content_draft`
import ครั้งเดียวจบผ่าน phpMyAdmin และรันซ้ำได้ไม่ทำข้อมูลเสีย

**สาม — ไม่รู้ว่าโฮสต์พร้อมหรือเปล่า**
`deploy/checkhost.php` ไปยืนบนโฮสต์แล้วถามคำถามเดียวกับที่ระบบจะถามตอนทำงานจริง
(รุ่น PHP · ส่วนขยาย · `.htaccess` ถูกอ่านไหม · เขียนทับ `index.html` ได้ไหม · HTTPS · ต่อฐานข้อมูลได้ไหม)
เปิดได้เฉพาะเมื่อใส่กุญแจที่สุ่มใหม่ทุกครั้งที่สร้างชุดไฟล์ ไม่ใส่ = 404

### หลักการของชุดไฟล์: เว็บจริงไม่ถูกแตะจนถึงขั้นสุดท้าย

`index.html` **ไม่อยู่ในชุดติดตั้ง** โดยตั้งใจ
ระบบเขียนไฟล์นั้นเองตอนกดปุ่มเผยแพร่ ซึ่งได้ผลสองอย่างในคราวเดียว:
เว็บเดิมปลอดภัยตลอดขั้นที่ 1–8 และการกดปุ่มนั้นคือบททดสอบสิทธิ์เขียนไฟล์ในตัว

`bin/make-bundle.php` ตรวจก่อนจบทุกครั้งว่า
(ก) `config.local.php` ไม่หลุดเข้าไป (ข) ไม่มีค่า `password` ที่ไม่ใช่ข้อความตัวอย่าง
(ค) ไฟล์ที่ต้องมี 26 รายการมีครบ (ง) ไฟล์ที่ต้องไม่มีไม่มีจริง — ไม่ผ่านข้อใดข้อหนึ่งคือ exit 1

> ข้อ (ค) เพิ่มเข้ามาเพราะรันครั้งแรกแล้ว `copy()` ลง `config/` ล้มเหลวเงียบ ๆ
> (ยังไม่ได้ `mkdir` ก่อน) ชุดไฟล์ออกมาครบทุกอย่าง **ยกเว้นไฟล์ที่คนต้องใช้ตั้งค่า**
> และไม่มีอะไรเตือนเลย · ทดสอบแล้วว่าถ้าย้อนการแก้กลับไป ตัวตรวจจับได้และหยุดด้วย exit 1

### ซ้อมติดตั้งจริง (`scratchpad/deploytest.js` — 43 ข้อ)

แตก `dist/*.zip` ลงโครงโฟลเดอร์แบบเดียวกับโฮสต์ (app/ อยู่นอก public_html)
ตั้ง `env = production` แล้วเดินตาม DEPLOY.md ทุกขั้นบนพอร์ตแยกต่างหาก

สิ่งที่พิสูจน์ได้: เส้นทาง `require` หากันเจอเมื่อ `app/` อยู่นอกโฟลเดอร์เว็บ ·
กดเผยแพร่แล้ว `index.html` ไปโผล่ใน `public_html` และ **ตรงกับที่ build ในเครื่องทุกไบต์** ·
`content.json` ไม่หลุดเข้าโฟลเดอร์เว็บ · ฟอร์มติดต่อ · `recover.php` และแถบเตือน ·
`env=production` ไม่มีรายละเอียดภายในหลุดออกมา

### บั๊กที่การซ้อมนี้จับได้ — การตรวจ Origin ของฟอร์มติดต่อ

`contact.php` เทียบ `$_SERVER['HTTP_HOST']` กับ `parse_url($origin, PHP_URL_HOST)` ตรง ๆ
ฝั่งแรกมีพอร์ตติดมาด้วยเมื่อไม่ใช่พอร์ตมาตรฐาน ฝั่งหลังตัดพอร์ตออกเสมอ
**ผลคือฟอร์มติดต่อถูกปฏิเสธทั้งหมด** บนทุกเว็บที่ไม่ได้อยู่บนพอร์ต 80/443

ชุดทดสอบเดิมมองไม่เห็น เพราะไม่ได้ส่งหัว `Origin` มา การตรวจจึงถูกข้ามไปทั้งก้อน
แก้โดยตัดพอร์ตออกจากทั้งสองฝั่งก่อนเทียบ และเทียบแบบไม่สนตัวพิมพ์
เพิ่มกรณีทดสอบทั้งฝั่งบวก (โดเมนตัวเองผ่าน) และฝั่งลบ (โดเมนอื่นถูกปฏิเสธ)

### คุกกี้ Secure หลัง reverse proxy

`Session::start()` ดูแค่ `$_SERVER['HTTPS']` ซึ่งโฮสต์ที่มี nginx หรือ Cloudflare
อยู่หน้า Apache จะไม่ตั้งค่านั้นให้ คุกกี้ล็อกอินจะไม่ติดธง `Secure`
แยกออกมาเป็น `Session::isHttps()` ที่ดู `X-Forwarded-Proto` และ `X-Forwarded-SSL` ด้วย
หัวพวกนี้ปลอมได้ แต่การปลอมทำได้แค่ "เพิ่ม" ธง Secure ซึ่งเข้มขึ้นไม่ใช่หลวมลง
จึงเชื่อได้อย่างปลอดภัยเฉพาะการตัดสินใจข้อนี้

### ไฟล์ที่เพิ่ม

| ไฟล์ | หน้าที่ |
|---|---|
| `bin/make-bundle.php` | ประกอบ `dist/` ทั้งหมด + ตรวจว่าไม่มีความลับหลุด |
| `deploy/checkhost.php` | ตรวจสภาพโฮสต์ก่อนติดตั้ง ใช้แล้วลบทิ้ง |
| `public/assets/media/.htaccess` | ห้ามรันสคริปต์ในโฟลเดอร์ที่ผู้ใช้อัปไฟล์เข้ามา (เนื้อหาตรงกับที่ `Media::ensureDirs()` สร้าง) |
| `DEPLOY.md` | เขียนใหม่ทั้งฉบับเป็นลำดับ 11 ขั้น + แผนย้อนกลับรายอาการ |

---

## 28. เก็บกวาดปุ่มที่กดแล้วไม่ไปไหน — ระยะที่ 1 (2026-09-17)

เว็บที่รับช่วงมามีจุดที่ "ดูเหมือนกดได้" อยู่ **20 จุด** แต่กดแล้วไม่เกิดอะไรขึ้น
ผู้ใช้เจอเองแล้วถามว่า "ของจริงมันก็ยังกดไม่ได้ละ แต่เราควรทำไว้ไหม"

| กลุ่ม | จำนวน | เดิม |
|---|---|---|
| การ์ดโซลูชัน "ดูเพิ่มเติม" | 6 | `<a href="#solutions">` — ลิงก์กลับมาที่หัวข้อที่การ์ดอยู่ |
| "ดูผลงานทั้งหมด" | 1 | `<a href="#projects">` — เช่นเดียวกัน |
| การ์ดบทความ "อ่านต่อ" | 3 | `<span>` เฉย ๆ ไม่ใช่ลิงก์เลย |
| "บทความทั้งหมด" | 1 | `<a href="#knowledge">` |
| ท้ายเว็บ คอลัมน์ "บริษัท" | 5 | `href="#"` |
| ท้ายเว็บ แถวข้อกำหนด | 4 | `href="#"` |

### สิ่งที่ทำในระยะที่ 1

**การ์ดโซลูชัน → เปิดฟอร์มติดต่อ**
เปลี่ยนจาก `<a>` เป็น `<button data-action="open-modal" data-solution="N">`
ข้อความเปลี่ยนจาก "ดูเพิ่มเติม" เป็น **"สอบถามเรื่องนี้"**
กดแล้วฟอร์มเปิดขึ้นพร้อมเลือกโซลูชันใบนั้นไว้ในช่อง "โซลูชันที่สนใจ" ให้เลย
จากปุ่มหลอกกลายเป็นช่องทางที่พาลูกค้าเข้ามาจริง โดยไม่ต้องสร้างหน้ารายละเอียดใหม่

ส่งเป็น **ลำดับที่** ไม่ใช่ข้อความ เพราะ `solutionOpts` เปลี่ยนตามภาษา
ถ้าเก็บข้อความไทยไว้แล้วผู้ใช้สลับเป็นอังกฤษ ช่องเลือกจะหาไม่เจอแล้วเด้งกลับเป็นค่าว่างเงียบ ๆ
`setLang()` จึงแปลงค่าตามภาษาใหม่ให้ด้วย

**ปุ่ม "ดูทั้งหมด" ทั้งสองปุ่ม → ถอดออก**
เว็บมีหน้าเดียว รายการทั้งหมดอยู่ใต้ปุ่มนั้นอยู่แล้ว ปุ่มจึงไม่มีอะไรจะพาไปดูเพิ่ม
ข้อความ `projects.viewAll` / `knowledge.viewAll` ยังอยู่ในเนื้อหา รอวันที่มีหน้ารวมจริง

**ลิงก์ท้ายเว็บ → มีช่องใส่ปลายทางของตัวเอง**
โครงเดิมเก็บเป็นข้อความเปล่า ๆ จึงไม่มีที่ให้ใส่ลิงก์เลย แก้ให้ถูกไม่ได้ถ้าไม่เปลี่ยนโครง
เปลี่ยนเป็น `{label, href}` แบบเดียวกับเมนูด้านบน ตั้งค่าได้จากหลังบ้าน
**รายการที่ยังไม่มีปลายทางจะไม่แสดงเลย** — ข้อความที่กดไม่ได้แย่กว่าไม่มีเลย
ตัวแสดงผลรองรับข้อมูลแบบเก่าที่เป็นข้อความเปล่าด้วย โดยถือว่ายังไม่มีลิงก์

ผลตอนนี้: คอลัมน์ "บริษัท" เหลือ 3 ลิงก์จริง (`#about` ×2, `#contact`)
แถวข้อกำหนดหายไปทั้งแถวเพราะยังไม่มีหน้านโยบายจริงสักหน้า

### บั๊กที่ชุดทดสอบจับได้

เปิดฟอร์มจากการ์ด → ปิด → กดปุ่ม "ขอรับคำปรึกษา" ทั่วไป
ฟอร์มยังค้างโซลูชันเดิมที่ระบบเติมให้ไว้ ลูกค้าจะส่งเรื่องเข้ามาโดยติดหัวข้อที่เขาไม่ได้เลือกเอง
แก้ให้ล้างเฉพาะค่าที่ระบบเติมให้ ส่วนที่ลูกค้าพิมพ์เองยังอยู่ครบ

### ผลกระทบต่อ verify.js

ข้อ 3 เคยยืนยันว่า "หน้าเว็บต้องเหมือนเว็บเดิมทุกตัวอักษร" ซึ่งงานนี้ตั้งใจให้ต่าง
ไม่ได้ทิ้งข้อนี้ แต่แปลงจุดที่ตั้งใจเปลี่ยนกลับก่อนเทียบ แล้วเทียบส่วนที่เหลือแบบไบต์ต่อไบต์เหมือนเดิม
ทุกการแปลง **นับจำนวนครั้งไว้** ถ้า regex หาไม่เจอหรือเจอไม่ครบ เทสต์จะฟ้องทันที
ไม่ใช่ไม่ match แล้วผ่านไปเงียบ ๆ จนกลายเป็นเทสต์ที่ไม่ได้ตรวจอะไรเลย

ส่วนท้ายเว็บแปลงกลับไม่ได้ (รายการที่ไม่มีปลายทางหายไปจริง) จึงตัดออกจากการเทียบตัวอักษร
แล้วเพิ่มข้อ 6 ตรวจพฤติกรรมแทน — แบบเดียวกับที่ข้อ 5 เคยทำกับฟอร์มติดต่อ

| ข้อ | ตรวจอะไร |
|---|---|
| 6 | ไม่มี `href="#"` เหลือทั้งหน้า · ส่วนท้ายเหลือเฉพาะลิงก์ที่มีปลายทาง · เว็บเดิมต้องเคยมีปัญหานี้จริง |
| 7 | การ์ด 6 ใบเปิดฟอร์มได้ · เลือกโซลูชันถูกใบ · สลับภาษาแล้วยังอยู่ · เปิดจากปุ่มทั่วไปต้องไม่มีอะไรถูกเลือกไว้ |

ข้อ 7 รันสคริปต์ของหน้าเว็บจริงแล้วเรียก `openModal(3)` จริง ไม่ใช่แค่ดูว่ามีปุ่มอยู่ในหน้า

### ไฟล์ที่เปลี่ยน

| ไฟล์ | เปลี่ยนอะไร |
|---|---|
| `template.html` | การ์ดโซลูชันเป็นปุ่ม · ถอดปุ่มดูทั้งหมด · ตัวแสดงลิงก์ท้ายเว็บ · `openModal(idx)` · `setLang()` |
| `content.json` | `footer.company` / `footer.legal` เป็น `{label, href}` · `cta.learnMore` |
| `bin/link-footer.php` | **ใหม่** — อัปเกรดฉบับร่างในฐานข้อมูลบนเว็บจริงให้ตรงกับโครงใหม่ (รันซ้ำได้) |
| `verify.js` | ข้อ 2 เทียบเฉพาะข้อความของลิงก์ท้ายเว็บ · ข้อ 3 แปลงจุดที่ตั้งใจเปลี่ยน · เพิ่มข้อ 6 และ 7 |

### ยังไม่ได้ทำ (ระยะที่ 2)

การ์ดบทความ "อ่านต่อ" ทั้ง 3 ใบ **ยังกดไม่ได้เหมือนเดิม**
เพราะเนื้อหายังเป็นข้อความตัวอย่างของนักพัฒนาคนเดิม
("ข้อความตัวอย่าง แทนที่ด้วยบทความจริงของ FMK...")
ทำปุ่มให้กดได้ก่อนมีบทความจริง จะแย่กว่าปล่อยไว้ — รอเจ้าของเว็บตัดสินใจเรื่องเนื้อหาก่อน

---

## 29. หน้านโยบายความเป็นส่วนตัว — หน้าที่สองของเว็บ (2026-09-17)

ตอนเก็บลิงก์ท้ายเว็บ (§28) เจอเรื่องที่ใหญ่กว่าลิงก์เสีย

ข้อความยินยอมในฟอร์มติดต่อเขียนว่า *"...ตามนโยบายความเป็นส่วนตัว"*
แต่ไม่เคยมีนโยบายให้อ่านเลยสักฉบับ ตาม PDPA การขอความยินยอมโดยอ้างถึงเอกสาร
ที่เจ้าของข้อมูลเข้าถึงไม่ได้ ถือว่าความยินยอมนั้นไม่สมบูรณ์
และปัญหานี้อยู่ตรงนั้นไม่ว่าจะแสดงคำว่า "นโยบายความเป็นส่วนตัว" ท้ายเว็บหรือไม่

เจ้าของเว็บเลือกให้ทำหน้านโยบายจริง ไม่ใช่ซ่อนคำนั้นทิ้ง

### เนื้อหาเขียนจากสิ่งที่ระบบทำจริง

ไม่ได้ลอกแม่แบบสำเร็จรูปมา แต่ไล่อ่านโค้ดแล้วเขียนตามที่เกิดขึ้นจริง

| ข้อเท็จจริง | มาจากไหน |
|---|---|
| ช่องที่เก็บ 11 ช่อง | `db/004_messages.sql` |
| เก็บ IP, user agent, ภาษา, เวลา | `Messages::submit()` |
| บันทึกการปฏิเสธแยกตาราง เก็บแค่ IP กับเหตุผล | `contact_blocked` |
| เว็บสาธารณะไม่มีคุกกี้เลย | `bootstrap.php` ไม่เรียก `Session::start()` · template ไม่มี `document.cookie` หรือ storage ใด ๆ |
| หลังบ้านมีคุกกี้เซสชันหนึ่งตัว | `Session::start()` |
| รหัสผ่านเก็บแบบแฮชทางเดียว | `Auth` (argon2id) |

**ข้อที่เกือบพลาด** — หน้าเว็บโหลดฟอนต์จาก `fonts.googleapis.com`
แปลว่าหมายเลขไอพีของผู้เข้าชมทุกคนไปถึงเซิร์ฟเวอร์ของ Google ตั้งแต่วินาทีที่เปิดหน้า
เป็นการเปิดเผยข้อมูลให้บุคคลที่สามที่นโยบายต้องบอก แม้เราจะไม่ได้ตั้งใจส่งอะไรให้ก็ตาม
ถ้าวันหนึ่งอยากตัดข้อนี้ออกจริง ๆ ต้องย้ายฟอนต์มาไว้บนเซิร์ฟเวอร์ตัวเอง

### สถาปัตยกรรม — หน้าที่สองโดยไม่ทำลายของเดิม

เดิมระบบคือ "แม่แบบหนึ่งไฟล์ + เนื้อหา JSON → หน้าเว็บหนึ่งหน้า"
ตอนนี้เป็น "แม่แบบหลายไฟล์ + เนื้อหาก้อนเดียวกัน → หลายหน้า"

`template-privacy.html` เป็นไฟล์แยก **จงใจไม่ยืมแม่แบบหน้าแรกมาทั้งก้อน**

เหตุผล — ถ้าใช้แม่แบบเดียวกันแล้วสลับด้วยธง จะได้ปัญหาสามข้อพร้อมกัน
`<head>` ที่ประกาศ canonical ชี้ไปหน้าแรก (Google จะถือว่าหน้านโยบายเป็นหน้าซ้ำแล้วไม่เก็บดัชนี) ·
หน้านโยบายต้องแบกโค้ดเรนเดอร์ของหน้าขายของทั้งหมด ·
และต้องไปแก้เส้นทางเรนเดอร์ของหน้าแรกซึ่งเพิ่งขึ้นเว็บจริงไปหมาด ๆ

แยกไฟล์แล้วหน้าแรกไม่ถูกแตะเลยแม้แต่บรรทัดเดียวในเรื่องนี้ แลกกับ CSS ชุดเล็กที่เขียนซ้ำ ~60 บรรทัด

### คัดเนื้อหาตามหน้า

ตอนแรกส่งเนื้อหาทั้งก้อนให้ทั้งสองหน้า ผลคือหน้าแรกโตขึ้น **17KB**
เพราะแบกข้อความนโยบายที่ไม่มีทางได้แสดง

แก้ด้วย `PAGE_I18N` ที่มีทั้งใน `build.js` และ `Builder.php` (ต้องตรงกันเป๊ะ)

| หน้า | ได้คีย์อะไร |
|---|---|
| `home` | ทุกคีย์ ยกเว้น `privacy` |
| `privacy` | เฉพาะ `privacy` และ `footer` |

`pickKeys()` ต้องคงลำดับคีย์เดิมไว้ เพราะ JSON ที่ออกมาต้องตรงกับฝั่ง Node ทุกไบต์
PHP `foreach` และ JS `Object.keys()` ต่างก็คงลำดับการใส่ จึงตรงกันโดยธรรมชาติ

ผลลัพธ์: หน้าแรก 100,456 bytes · หน้านโยบาย 27,548 bytes · ทั้งคู่ PHP กับ Node ตรงกันทุกไบต์

### เผยแพร่พร้อมกันเสมอ

`Builder::writePages()` เรนเดอร์ **ทั้งสองหน้าให้เสร็จก่อน** แล้วค่อยเขียนไฟล์
ถ้าหน้าใดหน้าหนึ่งเรนเดอร์ไม่ผ่านจะโยน exception ตั้งแต่ยังไม่ได้แตะไฟล์จริง
เว็บจึงไม่มีทางค้างในสภาพที่หน้าแรกเป็นเนื้อหาใหม่แต่หน้านโยบายยังเป็นของเก่า

ประวัติเวอร์ชันเก็บ **เนื้อหา JSON** ไม่ใช่ไฟล์ HTML การย้อนกลับจึงดึงเนื้อหาเก่ามาเป็นฉบับร่าง
แล้วเรนเดอร์ใหม่ทั้งสองหน้า — ไม่ต้องแก้อะไรในส่วนนี้เลย

### ภาษา

ลิงก์ที่ลงท้าย `.html` จะถูกต่อ `?lang=` ของภาษาที่กำลังอ่านอยู่โดยอัตโนมัติ (`pageHref()`)
คนที่อ่านหน้าแรกเป็นไทยกดไปหน้านโยบายจะได้ภาษาไทยต่อ ไม่เด้งเป็นอังกฤษ
ถ้าเข้าหน้านโยบายตรง ๆ โดยไม่มี `?lang=` จะดูจากภาษาเบราว์เซอร์ และตกลงที่ภาษาไทยเป็นค่าสุดท้าย
เพราะเป็นภาษาของกฎหมายที่เอกสารนี้อ้างถึง

### ไฟล์ที่เปลี่ยน

| ไฟล์ | เปลี่ยนอะไร |
|---|---|
| `template-privacy.html` | **ใหม่** — แม่แบบหน้านโยบาย พร้อม `<head>` และ CSS ของตัวเอง |
| `bin/add-privacy.php` | **ใหม่** — ใส่เนื้อหานโยบาย ลิงก์ท้ายเว็บ และลิงก์ใต้ช่องยินยอม (มีโหมด `--json` สำหรับเครื่องพัฒนา) |
| `build.js` · `app/Builder.php` | สร้างสองหน้า · `PAGE_I18N` คัดคีย์ตามหน้า · `writePages()` |
| `app/Content.php` · `public/admin/publish.php` | รายงานจำนวนไบต์ของทั้งสองหน้า |
| `public/admin/preview.php` | `?page=privacy` และปุ่มสลับดูสองหน้า |
| `template.html` | `pageHref()` ต่อ `?lang=` · ลิงก์นโยบายใต้ช่องยินยอม |
| `app/Sections.php` | กลุ่ม "หน้านโยบาย" และชื่อช่องภาษาไทยของฟิลด์ใหม่ |
| `bin/snapshot.php` | สำรองและกู้คืน `privacy.html` ด้วย |
| `bin/make-bundle.php` | รวม `template-privacy.html` เข้าชุด · ไม่ขน `privacy.html` ขึ้นไป (ระบบสร้างเอง) |
| `verify.js` | เพิ่มข้อ 8 · ข้อ 6 ปรับเพราะนโยบายมีปลายทางแล้ว |

### สองข้อที่ต้องให้เจ้าของเว็บยืนยันก่อนใช้จริง

1. **ระยะเวลาเก็บข้อมูล** — ตอนนี้เขียนว่า 2 ปีนับจากติดต่อครั้งล่าสุด และ 90 วันสำหรับบันทึกกันสแปม
   เป็นค่าที่พอสมเหตุสมผล แต่เป็นตัวเลขที่บริษัทต้องเลือกเอง ไม่ใช่นักพัฒนาเลือกให้
2. **ที่ตั้งเซิร์ฟเวอร์** — ถ้าโฮสต์อยู่นอกประเทศไทย PDPA กำหนดให้ต้องแจ้งการส่งข้อมูลข้ามแดน
   ตอนนี้นโยบายยังไม่ได้ระบุ เพราะยังไม่ทราบข้อเท็จจริง

เอกสารนี้เขียนจากสิ่งที่ระบบทำจริงและอิงโครงของ PDPA แต่ **ไม่ใช่ความเห็นทางกฎหมาย**
ก่อนใช้จริงควรให้ผู้มีความรู้ทางกฎหมายอ่านอีกรอบ

---

## 30. บทความ "อ่านต่อ" — มีสวิตช์ ปิดไว้ก่อน (2026-09-17)

จุดสุดท้ายจาก §28 ที่ค้างไว้ คำว่า "อ่านต่อ" บนการ์ดบทความทั้งสามใบ
เว็บเดิมโชว์คำนี้ไว้ทุกใบ แต่ไม่เคยกดได้เลย เพราะไม่มีที่เก็บเนื้อหาเต็มของบทความ

เจ้าของเว็บสั่งให้ทำกลไกไว้เลย โดยตั้งค่าเริ่มต้นเป็น "ปิด"
แล้วเขาจะไปใส่เนื้อหาจริงกับกดเปิดเองทีหลัง

### สองเงื่อนไข ไม่ใช่เงื่อนไขเดียว

การ์ดจะกดได้ต่อเมื่อ **เปิดสวิตช์** และ **มีย่อหน้าจริงอย่างน้อยหนึ่งย่อหน้า**

เช็กแค่สวิตช์ไม่พอ เพราะคนที่ติ๊กเปิดก่อนพิมพ์เนื้อหาจะได้หน้าต่างเปล่า
ซึ่งแย่กว่าไม่มีปุ่มเลย ป้ายชื่อช่องในหลังบ้านจึงเขียนเงื่อนไขนี้ไว้ตรง ๆ ว่า
*"เปิดให้กดอ่านเนื้อหาเต็ม (ต้องใส่ย่อหน้าอย่างน้อยหนึ่งย่อหน้าก่อน)"*

และเมื่อยังไม่พร้อม การ์ดจะ **ไม่มีคำว่า "อ่านต่อ" อยู่บนนั้นเลย** ไม่ใช่มีแล้วกดไม่ติด
ซึ่งเป็นกฎเดียวกับที่ใช้กับลิงก์ท้ายเว็บใน §28

### "ปิดอยู่" ต้องแปลว่าไม่มีใครเห็น

`dropUnreadBodies()` (มีทั้งใน `build.js` และ `Builder.php` ต้องตรงกัน)
ตัดเนื้อหาของบทความที่ยังปิดอยู่ออกจาก payload ก่อนฝังลงหน้าเว็บ

ถ้าไม่ทำ ข้อความที่ผู้ดูแลเขียนค้างไว้จะอยู่ในซอร์สของหน้าเว็บจริง
ใครกด View Source ก็อ่านได้ และ Google ก็เก็บไปเข้าดัชนีได้
ผู้ดูแลจึงเขียนบทความค้างไว้ได้อย่างปลอดภัย โดยไม่ต้องกลัวว่าฉบับร่างจะหลุด

### สวิตช์ต้องเหมือนกันทั้งสองภาษา

`readable` ไม่ใช่ข้อความ จึงถูกเพิ่มเข้าไปใน `copyNeutral()` ของหน้าแก้เนื้อหา
คู่กับ `k` และ `key` ที่ทำแบบเดียวกันอยู่แล้ว

ถ้าปล่อยให้ตั้งแยกกัน จะเกิดกรณีที่บทความกดอ่านได้เฉพาะภาษาอังกฤษ
โดยไม่มีอะไรฟ้อง — เป็นบั๊กประเภทที่หาเจอยากที่สุด

### หน้าต่างอ่านบทความ ไม่ใช่หน้าแยก

ใช้โครงเดียวกับหน้าต่างฟอร์มติดต่อ วางไว้ในหน้าเดิม พร้อมลิงก์ตรง `/#article-1`
ที่ส่งต่อให้คนอื่นเปิดอ่านได้ (ใช้ `history.replaceState` จะได้ไม่ทำให้ปุ่มย้อนกลับรก)
และมีปุ่ม "ขอรับคำปรึกษา" ปิดท้ายบทความ ซึ่งจะปิดหน้าต่างบทความก่อนเปิดฟอร์ม

**ข้อแลกเปลี่ยนที่ต้องรู้** — แต่ละบทความไม่มี URL ของตัวเอง ไม่มี title ของตัวเอง
Google จึงเก็บข้อความบทความไปเป็นส่วนหนึ่งของหน้าแรก ไม่ใช่เป็นหน้าอิสระ
ถ้าวันหนึ่งส่วนบทความโตจนเป็นคลังความรู้จริง ควรแยกเป็นหน้าละบทความ
ซึ่งทำได้แล้วเพราะ §29 เปิดทางให้ระบบสร้างได้หลายหน้า

### บั๊กที่เจอระหว่างทาง

§29 ทำให้ลิงก์ "กลับหน้าแรก" จากหน้านโยบายส่ง `?lang=th` มาด้วย
แต่ **หน้าแรกไม่เคยอ่านค่านั้น** คนที่อ่านภาษาไทยอยู่จึงถูกเด้งกลับเป็นอังกฤษทุกครั้งที่ข้ามหน้า
แก้โดยให้หน้าแรกรับ `?lang=` ตอนเริ่มทำงาน

### ไฟล์ที่เปลี่ยน

| ไฟล์ | เปลี่ยนอะไร |
|---|---|
| `bin/add-article-body.php` | **ใหม่** — เพิ่ม `readable` กับ `body` ให้บทความทุกชิ้น ปิดไว้ทั้งหมด |
| `template.html` | `articleReady()` · การ์ดเป็นปุ่มเมื่อพร้อม · `renderArticle()` · ลิงก์ตรง `#article-N` · รับ `?lang=` |
| `build.js` · `app/Builder.php` | `dropUnreadBodies()` |
| `app/Sections.php` | ป้ายชื่อของ `readable` |
| `public/admin/content.php` | `copyNeutral()` คัด `readable` ไปให้ภาษาไทยด้วย |
| `verify.js` | เพิ่มข้อ 9 · ข้อ 3 ถอดคำว่า "อ่านต่อ" ของเว็บเดิมออกก่อนเทียบ |

### สถานะตอนนี้

บทความทั้งสามชิ้น **ปิดอยู่ทั้งหมด** และยังเป็นข้อความตัวอย่างของนักพัฒนาคนเดิม
หน้าเว็บจึงไม่มีคำว่า "อ่านต่อ" ให้กดเลย ซึ่งตรงกับความเป็นจริงว่ายังไม่มีบทความให้อ่าน
